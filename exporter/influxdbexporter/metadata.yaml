type: influxdb

status:
  class: exporter
  stability:
    beta: [traces, metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [srebhan, jacobmarble]

tests:
  expect_consumer_error: true
