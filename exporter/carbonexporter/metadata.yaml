type: carbon

status:
  class: exporter
  stability:
    beta: [metrics]
  distributions: [contrib, observiq, sumo]
  codeowners:
    active: [aboguszewski-sumo]

tests:
  config:
  expect_consumer_error: true