type: kafka

status:
  class: exporter
  stability:
    beta: [traces, metrics, logs]
  distributions: [core, contrib]
  codeowners:
    active: [pavolloffay, MovieStoreGuy, axw]

tests:
  config:
  skip_lifecycle: true
