tinybird:
  endpoint: "https://api.tinybird.co"
  token: "test-token"

tinybird/full:
  endpoint: "https://api.tinybird.co"
  compression: "zstd"
  retry_on_failure:
    enabled: false
  sending_queue:
    enabled: false
  token: "test-token"
  metrics::gauge::datasource: "metrics_gauge"
  metrics::sum::datasource: "metrics_sum"
  metrics::histogram::datasource: "metrics_histogram"
  metrics::exponential_histogram::datasource: "metrics_exponential_histogram"
  traces::datasource: "traces"
  logs::datasource: "logs"
  wait: true

tinybird/invalid_datasource:
  endpoint: "https://api.tinybird.co"
  token: "test-token"
  metrics::gauge::datasource: "metrics-with-dashes"
  metrics::sum::datasource: "metrics-with-dashes"
  metrics::histogram::datasource: "metrics-with-dashes"
  metrics::exponential_histogram::datasource: "metrics-with-dashes"
  traces::datasource: "traces-with-dashes"
  logs::datasource: "logs-with-dashes"

tinybird/missing_token:
  endpoint: "https://api.tinybird.co"

tinybird/missing_endpoint:
  token: "test-token"
