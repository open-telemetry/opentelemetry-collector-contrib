type: elasticsearch
scope_name: otelcol/elasticsearch

status:
  class: exporter
  stability:
    beta: [traces, logs]
  distributions: [contrib, observiq]
  codeowners:
    active: [JaredTan95]

tests:
  config:
    endpoints: [http://localhost:9200]