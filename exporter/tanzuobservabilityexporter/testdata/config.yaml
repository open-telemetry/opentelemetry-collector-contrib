receivers:
  nop:

processors:
  nop:

exporters:
  tanzuobservability:
    traces:
      endpoint: "http://localhost:40001"
      default_application: an_application
      default_service: a_service

service:
  pipelines:
    traces:
      receivers: [ nop ]
      processors: [ nop ]
      exporters: [ tanzuobservability ]
