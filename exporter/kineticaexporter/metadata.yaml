type: kinetica

status:
  class: exporter
  stability:
    development: [metrics, traces, logs]
  distributions: []
  codeowners:
    active: [am-kinetica, TylerHelmuth]

tests:
  config:
  skip_lifecycle: true