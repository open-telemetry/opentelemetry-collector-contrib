receivers:
  nop:

processors:
  nop:

exporters:
  filelog:
    path: ./log
    max_open_files: -1

service:
  pipelines:
    logs:
      receivers: [nop]
      processors: [nop]
      exporters: [filelog]
