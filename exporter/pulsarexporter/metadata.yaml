type: pulsar

status:
  class: exporter
  stability:
    alpha: [traces, metrics, logs]
  distributions: [contrib, splunk]
  codeowners:
    active: [dmitryax, dao-jun]

tests:
  config:
  skip_lifecycle: true