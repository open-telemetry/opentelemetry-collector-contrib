type: sumologic

status:
  class: exporter
  stability:
    beta: [metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [sumo-drosiek]

# TODO: Update the exporter to pass the tests
tests:
  skip_lifecycle: true
