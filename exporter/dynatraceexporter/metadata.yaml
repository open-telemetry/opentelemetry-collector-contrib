type: dynatrace

status:
  class: exporter
  stability:
    deprecated: [metrics]
  distributions: [contrib, aws, observiq]
  codeowners:
    active: [dyladan, arminru, evan-bradley]

tests:
  config:
  expect_consumer_error: true
