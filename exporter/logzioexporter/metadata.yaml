type: logzio

status:
  class: exporter
  stability:
    beta: [traces, logs]
  distributions: [contrib]
  codeowners:
    active: [yotamloe]

tests:
  config:
    endpoint: "172.0.0.1:8080:"
  expect_consumer_error: true