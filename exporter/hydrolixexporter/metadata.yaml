type: hydrolix

status:
  class: exporter
  stability:
    development: [metrics, traces, logs]
  codeowners:
    active: [murphy]

tests:
  config:
    endpoint: http://localhost:8080
    hdx_table: test_table
    hdx_transform: test_transform
    hdx_username: test_user
    hdx_password: test_pass
