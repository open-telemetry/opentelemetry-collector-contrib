receivers:
  nop:

processors:
  nop:

exporters:
  azureblob:
  azureblob/2:
    connection_string: DefaultEndpointsProtocol=https;AccountName=accountName;AccountKey=+idLkHYcL0MUWIKYHm2j4Q==;EndpointSuffix=core.windows.net
    logs_container_name: logs
    traces_container_name: traces

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [azureblob]
    logs:
      receivers: [nop]
      processors: [nop]
      exporters: [azureblob]
