receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  datadog:
    sending_method: agent
    tags:
      - tool:opentelemetry
      - version:0.1.0
  datadog/2:
    api_key: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
    site: datadoghq.eu
  datadog/invalid:
     api_key: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
     sending_method: agent

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [datadog, datadog/2]
