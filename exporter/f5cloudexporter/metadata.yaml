type: f5cloud

status:
  class: exporter
  stability:
    deprecated: [traces, metrics, logs]
  distributions: [contrib, observiq]
  codeowners:
    active: []
    emeritus: [gramidt]

tests:
  config:
    endpoint: "https://f5cloud"
    source: "dev"
    f5cloud_auth:
      credential_file: "testdata/empty_credential_file.json"
  skip_lifecycle: true
