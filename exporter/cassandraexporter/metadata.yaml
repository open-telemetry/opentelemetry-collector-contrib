type: cassandra

status:
  class: exporter
  stability:
    alpha: [traces, logs]
  distributions: [contrib]
  codeowners:
    active: [atoulme]

# TODO: Update the exporter to pass the tests
tests:
  skip_lifecycle: true