type: clickhouse

status:
  class: exporter
  stability:
    alpha: [traces, metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [hanjm, dmitryax, Frapschen]

tests:
  config:
  skip_lifecycle: true
