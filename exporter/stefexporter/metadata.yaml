type: stef

status:
  class: exporter
  stability:
    development: [metrics]
  distributions: []
  codeowners:
    active: [tigrannajaryan, dmitryax]
tests:
  config:
    endpoint: "http://localhost:0"
  expect_consumer_error: true
