receivers:
  examplereceiver:

processors:

exporters:
  loadbalancing:
    config: 

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors: []
      exporters:
      - loadbalancing
