type: otelarrow
display_name: OpenTelemetry Arrow Exporter

status:
  class: exporter
  stability:
    beta: [traces, metrics, logs]
  distributions: [contrib, k8s]
  codeowners:
    active: [jmacd, lquerel, JakeDern]

tests:
  config:
    endpoint: http://127.0.0.1:4317
