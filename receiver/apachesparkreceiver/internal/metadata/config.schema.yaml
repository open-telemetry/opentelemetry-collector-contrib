# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for apachespark metrics.
    type: object
    properties:
      spark.driver.block_manager.disk.usage:
        description: "SparkDriverBlockManagerDiskUsageConfig provides config for the spark.driver.block_manager.disk.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.block_manager.memory.usage:
        description: "SparkDriverBlockManagerMemoryUsageConfig provides config for the spark.driver.block_manager.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.compilation.average_time:
        description: "SparkDriverCodeGeneratorCompilationAverageTimeConfig provides config for the spark.driver.code_generator.compilation.average_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.compilation.count:
        description: "SparkDriverCodeGeneratorCompilationCountConfig provides config for the spark.driver.code_generator.compilation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.generated_class.average_size:
        description: "SparkDriverCodeGeneratorGeneratedClassAverageSizeConfig provides config for the spark.driver.code_generator.generated_class.average_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.generated_class.count:
        description: "SparkDriverCodeGeneratorGeneratedClassCountConfig provides config for the spark.driver.code_generator.generated_class.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.generated_method.average_size:
        description: "SparkDriverCodeGeneratorGeneratedMethodAverageSizeConfig provides config for the spark.driver.code_generator.generated_method.average_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.generated_method.count:
        description: "SparkDriverCodeGeneratorGeneratedMethodCountConfig provides config for the spark.driver.code_generator.generated_method.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.source_code.average_size:
        description: "SparkDriverCodeGeneratorSourceCodeAverageSizeConfig provides config for the spark.driver.code_generator.source_code.average_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.code_generator.source_code.operations:
        description: "SparkDriverCodeGeneratorSourceCodeOperationsConfig provides config for the spark.driver.code_generator.source_code.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.dag_scheduler.job.active:
        description: "SparkDriverDagSchedulerJobActiveConfig provides config for the spark.driver.dag_scheduler.job.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.dag_scheduler.job.count:
        description: "SparkDriverDagSchedulerJobCountConfig provides config for the spark.driver.dag_scheduler.job.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.dag_scheduler.stage.count:
        description: "SparkDriverDagSchedulerStageCountConfig provides config for the spark.driver.dag_scheduler.stage.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.dag_scheduler.stage.failed:
        description: "SparkDriverDagSchedulerStageFailedConfig provides config for the spark.driver.dag_scheduler.stage.failed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.executor.gc.operations:
        description: "SparkDriverExecutorGcOperationsConfig provides config for the spark.driver.executor.gc.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.executor.gc.time:
        description: "SparkDriverExecutorGcTimeConfig provides config for the spark.driver.executor.gc.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.executor.memory.execution:
        description: "SparkDriverExecutorMemoryExecutionConfig provides config for the spark.driver.executor.memory.execution metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.executor.memory.jvm:
        description: "SparkDriverExecutorMemoryJvmConfig provides config for the spark.driver.executor.memory.jvm metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.executor.memory.pool:
        description: "SparkDriverExecutorMemoryPoolConfig provides config for the spark.driver.executor.memory.pool metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.executor.memory.storage:
        description: "SparkDriverExecutorMemoryStorageConfig provides config for the spark.driver.executor.memory.storage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.hive_external_catalog.file_cache_hits:
        description: "SparkDriverHiveExternalCatalogFileCacheHitsConfig provides config for the spark.driver.hive_external_catalog.file_cache_hits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.hive_external_catalog.files_discovered:
        description: "SparkDriverHiveExternalCatalogFilesDiscoveredConfig provides config for the spark.driver.hive_external_catalog.files_discovered metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.hive_external_catalog.hive_client_calls:
        description: "SparkDriverHiveExternalCatalogHiveClientCallsConfig provides config for the spark.driver.hive_external_catalog.hive_client_calls metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.hive_external_catalog.parallel_listing_jobs:
        description: "SparkDriverHiveExternalCatalogParallelListingJobsConfig provides config for the spark.driver.hive_external_catalog.parallel_listing_jobs metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.hive_external_catalog.partitions_fetched:
        description: "SparkDriverHiveExternalCatalogPartitionsFetchedConfig provides config for the spark.driver.hive_external_catalog.partitions_fetched metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.jvm_cpu_time:
        description: "SparkDriverJvmCPUTimeConfig provides config for the spark.driver.jvm_cpu_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.live_listener_bus.dropped:
        description: "SparkDriverLiveListenerBusDroppedConfig provides config for the spark.driver.live_listener_bus.dropped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.live_listener_bus.posted:
        description: "SparkDriverLiveListenerBusPostedConfig provides config for the spark.driver.live_listener_bus.posted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.live_listener_bus.processing_time.average:
        description: "SparkDriverLiveListenerBusProcessingTimeAverageConfig provides config for the spark.driver.live_listener_bus.processing_time.average metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.driver.live_listener_bus.queue_size:
        description: "SparkDriverLiveListenerBusQueueSizeConfig provides config for the spark.driver.live_listener_bus.queue_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.disk.usage:
        description: "SparkExecutorDiskUsageConfig provides config for the spark.executor.disk.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.gc_time:
        description: "SparkExecutorGcTimeConfig provides config for the spark.executor.gc_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.input_size:
        description: "SparkExecutorInputSizeConfig provides config for the spark.executor.input_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.memory.usage:
        description: "SparkExecutorMemoryUsageConfig provides config for the spark.executor.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.shuffle.io.size:
        description: "SparkExecutorShuffleIoSizeConfig provides config for the spark.executor.shuffle.io.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.storage_memory.usage:
        description: "SparkExecutorStorageMemoryUsageConfig provides config for the spark.executor.storage_memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.task.active:
        description: "SparkExecutorTaskActiveConfig provides config for the spark.executor.task.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.task.limit:
        description: "SparkExecutorTaskLimitConfig provides config for the spark.executor.task.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.task.result:
        description: "SparkExecutorTaskResultConfig provides config for the spark.executor.task.result metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.executor.time:
        description: "SparkExecutorTimeConfig provides config for the spark.executor.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.job.stage.active:
        description: "SparkJobStageActiveConfig provides config for the spark.job.stage.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.job.stage.result:
        description: "SparkJobStageResultConfig provides config for the spark.job.stage.result metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.job.task.active:
        description: "SparkJobTaskActiveConfig provides config for the spark.job.task.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.job.task.result:
        description: "SparkJobTaskResultConfig provides config for the spark.job.task.result metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.disk.spilled:
        description: "SparkStageDiskSpilledConfig provides config for the spark.stage.disk.spilled metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.executor.cpu_time:
        description: "SparkStageExecutorCPUTimeConfig provides config for the spark.stage.executor.cpu_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.executor.run_time:
        description: "SparkStageExecutorRunTimeConfig provides config for the spark.stage.executor.run_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.io.records:
        description: "SparkStageIoRecordsConfig provides config for the spark.stage.io.records metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.io.size:
        description: "SparkStageIoSizeConfig provides config for the spark.stage.io.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.jvm_gc_time:
        description: "SparkStageJvmGcTimeConfig provides config for the spark.stage.jvm_gc_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.memory.peak:
        description: "SparkStageMemoryPeakConfig provides config for the spark.stage.memory.peak metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.memory.spilled:
        description: "SparkStageMemorySpilledConfig provides config for the spark.stage.memory.spilled metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.blocks_fetched:
        description: "SparkStageShuffleBlocksFetchedConfig provides config for the spark.stage.shuffle.blocks_fetched metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.fetch_wait_time:
        description: "SparkStageShuffleFetchWaitTimeConfig provides config for the spark.stage.shuffle.fetch_wait_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.io.disk:
        description: "SparkStageShuffleIoDiskConfig provides config for the spark.stage.shuffle.io.disk metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.io.read.size:
        description: "SparkStageShuffleIoReadSizeConfig provides config for the spark.stage.shuffle.io.read.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.io.records:
        description: "SparkStageShuffleIoRecordsConfig provides config for the spark.stage.shuffle.io.records metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.io.write.size:
        description: "SparkStageShuffleIoWriteSizeConfig provides config for the spark.stage.shuffle.io.write.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.shuffle.write_time:
        description: "SparkStageShuffleWriteTimeConfig provides config for the spark.stage.shuffle.write_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.status:
        description: "SparkStageStatusConfig provides config for the spark.stage.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.task.active:
        description: "SparkStageTaskActiveConfig provides config for the spark.stage.task.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.task.result:
        description: "SparkStageTaskResultConfig provides config for the spark.stage.task.result metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      spark.stage.task.result_size:
        description: "SparkStageTaskResultSizeConfig provides config for the spark.stage.task.result_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for apachespark resource attributes.
    type: object
    properties:
      spark.application.id:
        description: ResourceAttributeConfig provides common config for a spark.application.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      spark.application.name:
        description: ResourceAttributeConfig provides common config for a spark.application.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      spark.executor.id:
        description: ResourceAttributeConfig provides common config for a spark.executor.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      spark.job.id:
        description: ResourceAttributeConfig provides common config for a spark.job.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      spark.stage.attempt.id:
        description: ResourceAttributeConfig provides common config for a spark.stage.attempt.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      spark.stage.id:
        description: ResourceAttributeConfig provides common config for a spark.stage.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for apachespark metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
