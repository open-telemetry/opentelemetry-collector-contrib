receivers:
  windowsperfcounters:
    metric_metadata:
      - metric_name: metric
        description: desc
        unit: "1"
        gauge:
          value_type: double
processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [windowsperfcounters]
      processors: [nop]
      exporters: [nop]
