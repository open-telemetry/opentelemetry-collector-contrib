receivers:
  windowsperfcounters:
    metric_metadata:
      metric:
        description: desc
        unit: "1"
        gauge:
          value_type: double
    perfcounters:
      - object: "object"
        counters:
          - name: counter1
            metric: metric
  windowsperfcounters/customname:
    metric_metadata:
      metric:
        description: desc
        unit: "1"
        gauge:
          value_type: double
      metric2:
        description: desc
        unit: "1"
        gauge:
          value_type: double
    collection_interval: 30s
    perfcounters:
      - object: object1
        counters:
          - name: counter1
            metric: metric
      - object: object2
        counters:
          - name: counter1
            metric: metric
          - name: counter2
            metric: metric2

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [windowsperfcounters]
      processors: [nop]
      exporters: [nop]
      