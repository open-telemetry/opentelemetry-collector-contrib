receivers:
  windowsperfcounters:
    metric_metadata:
      - description: desc
        unit: "1"
        gauge:
          value_type: double
    perfcounters:
      - object: "object"
        counters:
          - name: counter1
            metric: metric

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [windowsperfcounters]
      processors: [nop]
      exporters: [nop]
