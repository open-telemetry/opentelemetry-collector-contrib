receivers:
  windowsperfcounters:
    metric_metadata:
      metric:
        description: desc
        unit: "1"
        gauge:
          value_type: double
    perfcounters:
      - object: "object"

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [windowsperfcounters]
      processors: [nop]
      exporters: [nop]
