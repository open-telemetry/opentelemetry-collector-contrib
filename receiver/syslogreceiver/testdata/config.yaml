receivers:
  syslog:
    tcp:
      listen_address: "0.0.0.0:29018"
    protocol: rfc5424

processors:
  exampleprocessor:

exporters:
  exampleexporter:

service:
  pipelines:
    logs:
      receivers: [syslog]
      processors: [exampleprocessor]
      exporters: [exampleexporter]
