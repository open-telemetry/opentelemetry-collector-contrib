type: syslog

status:
  class: receiver
  stability:
    alpha: [logs]
  distributions: [contrib, splunk, observiq, sumo]
  codeowners:
    active: [djaglowski]

tests:
  config:
    tcp:
      listen_address: "0.0.0.0:0"
    protocol: "rfc5424"
