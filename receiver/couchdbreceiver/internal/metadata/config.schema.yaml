# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for couchdb metrics.
    type: object
    properties:
      couchdb.average_request_time:
        description: "CouchdbAverageRequestTimeConfig provides config for the couchdb.average_request_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.database.open:
        description: "CouchdbDatabaseOpenConfig provides config for the couchdb.database.open metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.database.operations:
        description: "CouchdbDatabaseOperationsConfig provides config for the couchdb.database.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.file_descriptor.open:
        description: "CouchdbFileDescriptorOpenConfig provides config for the couchdb.file_descriptor.open metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.httpd.bulk_requests:
        description: "CouchdbHttpdBulkRequestsConfig provides config for the couchdb.httpd.bulk_requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.httpd.requests:
        description: "CouchdbHttpdRequestsConfig provides config for the couchdb.httpd.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.httpd.responses:
        description: "CouchdbHttpdResponsesConfig provides config for the couchdb.httpd.responses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      couchdb.httpd.views:
        description: "CouchdbHttpdViewsConfig provides config for the couchdb.httpd.views metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for couchdb resource attributes.
    type: object
    properties:
      couchdb.node.name:
        description: ResourceAttributeConfig provides common config for a couchdb.node.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for couchdb metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
