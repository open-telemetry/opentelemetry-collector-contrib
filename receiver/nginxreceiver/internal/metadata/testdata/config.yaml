default:
all_set:
  metrics:
    nginx.connections_accepted:
      enabled: true
    nginx.connections_current:
      enabled: true
    nginx.connections_handled:
      enabled: true
    nginx.requests:
      enabled: true
  resource_attributes:
none_set:
  metrics:
    nginx.connections_accepted:
      enabled: false
    nginx.connections_current:
      enabled: false
    nginx.connections_handled:
      enabled: false
    nginx.requests:
      enabled: false
  resource_attributes:
