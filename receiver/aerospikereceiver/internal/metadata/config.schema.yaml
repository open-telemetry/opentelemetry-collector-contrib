# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for aerospike metrics.
    type: object
    properties:
      aerospike.namespace.disk.available:
        description: "AerospikeNamespaceDiskAvailableConfig provides config for the aerospike.namespace.disk.available metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.namespace.geojson.region_query_cells:
        description: "AerospikeNamespaceGeojsonRegionQueryCellsConfig provides config for the aerospike.namespace.geojson.region_query_cells metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.namespace.geojson.region_query_false_positive:
        description: "AerospikeNamespaceGeojsonRegionQueryFalsePositiveConfig provides config for the aerospike.namespace.geojson.region_query_false_positive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.namespace.geojson.region_query_points:
        description: "AerospikeNamespaceGeojsonRegionQueryPointsConfig provides config for the aerospike.namespace.geojson.region_query_points metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.namespace.geojson.region_query_requests:
        description: "AerospikeNamespaceGeojsonRegionQueryRequestsConfig provides config for the aerospike.namespace.geojson.region_query_requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.namespace.memory.free:
        description: "AerospikeNamespaceMemoryFreeConfig provides config for the aerospike.namespace.memory.free metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.namespace.memory.usage:
        description: "AerospikeNamespaceMemoryUsageConfig provides config for the aerospike.namespace.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "component"
            default:
              - "component"
      aerospike.namespace.query.count:
        description: "AerospikeNamespaceQueryCountConfig provides config for the aerospike.namespace.query.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
                - "index"
                - "result"
            default:
              - "type"
              - "index"
              - "result"
      aerospike.namespace.scan.count:
        description: "AerospikeNamespaceScanCountConfig provides config for the aerospike.namespace.scan.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
                - "result"
            default:
              - "type"
              - "result"
      aerospike.namespace.transaction.count:
        description: "AerospikeNamespaceTransactionCountConfig provides config for the aerospike.namespace.transaction.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
                - "result"
            default:
              - "type"
              - "result"
      aerospike.node.connection.count:
        description: "AerospikeNodeConnectionCountConfig provides config for the aerospike.node.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
                - "operation"
            default:
              - "type"
              - "operation"
      aerospike.node.connection.open:
        description: "AerospikeNodeConnectionOpenConfig provides config for the aerospike.node.connection.open metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
            default:
              - "type"
      aerospike.node.memory.free:
        description: "AerospikeNodeMemoryFreeConfig provides config for the aerospike.node.memory.free metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aerospike.node.query.tracked:
        description: "AerospikeNodeQueryTrackedConfig provides config for the aerospike.node.query.tracked metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for aerospike resource attributes.
    type: object
    properties:
      aerospike.namespace:
        description: ResourceAttributeConfig provides common config for a aerospike.namespace resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      aerospike.node.name:
        description: ResourceAttributeConfig provides common config for a aerospike.node.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for aerospike metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
