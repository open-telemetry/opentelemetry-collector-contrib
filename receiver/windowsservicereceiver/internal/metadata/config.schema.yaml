# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for windowsservice metrics.
    type: object
    properties:
      windows.service.status:
        description: "WindowsServiceStatusConfig provides config for the windows.service.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for windowsservice metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
