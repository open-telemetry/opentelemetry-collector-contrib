default:
all_set:
  metrics:
    cisco.collector.duration.seconds:
      enabled: true
    cisco.device.up:
      enabled: true
    cisco.system.cpu.utilization:
      enabled: true
    cisco.system.memory.utilization:
      enabled: true
  resource_attributes:
    cisco.device.ip:
      enabled: true
    cisco.device.model:
      enabled: true
    cisco.device.name:
      enabled: true
none_set:
  metrics:
    cisco.collector.duration.seconds:
      enabled: false
    cisco.device.up:
      enabled: false
    cisco.system.cpu.utilization:
      enabled: false
    cisco.system.memory.utilization:
      enabled: false
  resource_attributes:
    cisco.device.ip:
      enabled: false
    cisco.device.model:
      enabled: false
    cisco.device.name:
      enabled: false
filter_set_include:
  resource_attributes:
    cisco.device.ip:
      enabled: true
      metrics_include:
        - regexp: ".*"
    cisco.device.model:
      enabled: true
      metrics_include:
        - regexp: ".*"
    cisco.device.name:
      enabled: true
      metrics_include:
        - regexp: ".*"
filter_set_exclude:
  resource_attributes:
    cisco.device.ip:
      enabled: true
      metrics_exclude:
        - strict: "cisco.device.ip-val"
    cisco.device.model:
      enabled: true
      metrics_exclude:
        - strict: "cisco.device.model-val"
    cisco.device.name:
      enabled: true
      metrics_exclude:
        - strict: "cisco.device.name-val"
