type: awscontainerinsightreceiver

status:
  class: receiver
  stability:
    beta: [metrics]
  distributions: [contrib, aws, observiq, sumo]
  warnings: [Other]
  codeowners:
    active: [Aneurysm9, pxaws]

tests:
  config:
  skip_lifecycle: true
