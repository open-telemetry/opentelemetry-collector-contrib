type: azureeventhub

status:
  class: receiver
  stability:
    alpha: [metrics, logs]
  distributions: [contrib, splunk, observiq, sumo]
  codeowners:
    active: [atoulme, djaglowski, cparkins]

tests:
  config:
  skip_lifecycle: true
