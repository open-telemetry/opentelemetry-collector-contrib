# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for oracledb metrics.
    type: object
    properties:
      oracledb.buffer_cache.utilization:
        description: "OracledbBufferCacheUtilizationMetricConfig provides config for the oracledb.buffer_cache.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.consistent_gets:
        description: "OracledbConsistentGetsMetricConfig provides config for the oracledb.consistent_gets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.cpu_time:
        description: "OracledbCPUTimeMetricConfig provides config for the oracledb.cpu_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.data_dictionary.hit_ratio:
        description: "OracledbDataDictionaryHitRatioMetricConfig provides config for the oracledb.data_dictionary.hit_ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.database.cpu.utilization:
        description: "OracledbDatabaseCPUUtilizationMetricConfig provides config for the oracledb.database.cpu.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.database.wait.utilization:
        description: "OracledbDatabaseWaitUtilizationMetricConfig provides config for the oracledb.database.wait.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.db_block_gets:
        description: "OracledbDbBlockGetsMetricConfig provides config for the oracledb.db_block_gets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.ddl_statements_parallelized:
        description: "OracledbDdlStatementsParallelizedMetricConfig provides config for the oracledb.ddl_statements_parallelized metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.dml_locks.limit:
        description: "OracledbDmlLocksLimitMetricConfig provides config for the oracledb.dml_locks.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.dml_locks.usage:
        description: "OracledbDmlLocksUsageMetricConfig provides config for the oracledb.dml_locks.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.dml_statements_parallelized:
        description: "OracledbDmlStatementsParallelizedMetricConfig provides config for the oracledb.dml_statements_parallelized metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.enqueue_deadlocks:
        description: "OracledbEnqueueDeadlocksMetricConfig provides config for the oracledb.enqueue_deadlocks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_locks.limit:
        description: "OracledbEnqueueLocksLimitMetricConfig provides config for the oracledb.enqueue_locks.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_locks.usage:
        description: "OracledbEnqueueLocksUsageMetricConfig provides config for the oracledb.enqueue_locks.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_resources.limit:
        description: "OracledbEnqueueResourcesLimitMetricConfig provides config for the oracledb.enqueue_resources.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_resources.usage:
        description: "OracledbEnqueueResourcesUsageMetricConfig provides config for the oracledb.enqueue_resources.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.exchange_deadlocks:
        description: "OracledbExchangeDeadlocksMetricConfig provides config for the oracledb.exchange_deadlocks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.execution.utilization:
        description: "OracledbExecutionUtilizationMetricConfig provides config for the oracledb.execution.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "oracledb.parse.type"
            default:
              - "oracledb.parse.type"
      oracledb.executions:
        description: "OracledbExecutionsMetricConfig provides config for the oracledb.executions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.hard_parses:
        description: "OracledbHardParsesMetricConfig provides config for the oracledb.hard_parses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.host.cpu.utilization:
        description: "OracledbHostCPUUtilizationMetricConfig provides config for the oracledb.host.cpu.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.library_cache.utilization:
        description: "OracledbLibraryCacheUtilizationMetricConfig provides config for the oracledb.library_cache.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.logical_reads:
        description: "OracledbLogicalReadsMetricConfig provides config for the oracledb.logical_reads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.logons:
        description: "OracledbLogonsMetricConfig provides config for the oracledb.logons metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_1_to_25_pct:
        description: "OracledbParallelOperationsDowngraded1To25PctMetricConfig provides config for the oracledb.parallel_operations_downgraded_1_to_25_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_25_to_50_pct:
        description: "OracledbParallelOperationsDowngraded25To50PctMetricConfig provides config for the oracledb.parallel_operations_downgraded_25_to_50_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_50_to_75_pct:
        description: "OracledbParallelOperationsDowngraded50To75PctMetricConfig provides config for the oracledb.parallel_operations_downgraded_50_to_75_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_75_to_99_pct:
        description: "OracledbParallelOperationsDowngraded75To99PctMetricConfig provides config for the oracledb.parallel_operations_downgraded_75_to_99_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_to_serial:
        description: "OracledbParallelOperationsDowngradedToSerialMetricConfig provides config for the oracledb.parallel_operations_downgraded_to_serial metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_not_downgraded:
        description: "OracledbParallelOperationsNotDowngradedMetricConfig provides config for the oracledb.parallel_operations_not_downgraded metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parse.rate:
        description: "OracledbParseRateMetricConfig provides config for the oracledb.parse.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "oracledb.parse.result"
            default:
              - "oracledb.parse.result"
      oracledb.parse.utilization:
        description: "OracledbParseUtilizationMetricConfig provides config for the oracledb.parse.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parse_calls:
        description: "OracledbParseCallsMetricConfig provides config for the oracledb.parse_calls metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.pga_memory:
        description: "OracledbPgaMemoryMetricConfig provides config for the oracledb.pga_memory metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.physical_read_io_requests:
        description: "OracledbPhysicalReadIoRequestsMetricConfig provides config for the oracledb.physical_read_io_requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_reads:
        description: "OracledbPhysicalReadsMetricConfig provides config for the oracledb.physical_reads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.physical_reads_direct:
        description: "OracledbPhysicalReadsDirectMetricConfig provides config for the oracledb.physical_reads_direct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_write_io_requests:
        description: "OracledbPhysicalWriteIoRequestsMetricConfig provides config for the oracledb.physical_write_io_requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_writes:
        description: "OracledbPhysicalWritesMetricConfig provides config for the oracledb.physical_writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_writes_direct:
        description: "OracledbPhysicalWritesDirectMetricConfig provides config for the oracledb.physical_writes_direct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.processes.limit:
        description: "OracledbProcessesLimitMetricConfig provides config for the oracledb.processes.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.processes.usage:
        description: "OracledbProcessesUsageMetricConfig provides config for the oracledb.processes.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.queries_parallelized:
        description: "OracledbQueriesParallelizedMetricConfig provides config for the oracledb.queries_parallelized metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.recycle_bin.limit:
        description: "OracledbRecycleBinLimitMetricConfig provides config for the oracledb.recycle_bin.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.redo_allocation.utilization:
        description: "OracledbRedoAllocationUtilizationMetricConfig provides config for the oracledb.redo_allocation.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.sessions.limit:
        description: "OracledbSessionsLimitMetricConfig provides config for the oracledb.sessions.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.sessions.usage:
        description: "OracledbSessionsUsageMetricConfig provides config for the oracledb.sessions.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "session_type"
                - "session_status"
            default:
              - "session_type"
              - "session_status"
      oracledb.shared_pool.utilization:
        description: "OracledbSharedPoolUtilizationMetricConfig provides config for the oracledb.shared_pool.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.sort.ratio:
        description: "OracledbSortRatioMetricConfig provides config for the oracledb.sort.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "oracledb.sort.type"
            default:
              - "oracledb.sort.type"
      oracledb.sql_service.response.duration:
        description: "OracledbSQLServiceResponseDurationMetricConfig provides config for the oracledb.sql_service.response.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.storage.usage:
        description: "OracledbStorageUsageMetricConfig provides config for the oracledb.storage.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.storage.utilization:
        description: "OracledbStorageUtilizationMetricConfig provides config for the oracledb.storage.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.tablespace_size.limit:
        description: "OracledbTablespaceSizeLimitMetricConfig provides config for the oracledb.tablespace_size.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "tablespace_name"
            default:
              - "tablespace_name"
      oracledb.tablespace_size.usage:
        description: "OracledbTablespaceSizeUsageMetricConfig provides config for the oracledb.tablespace_size.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "tablespace_name"
            default:
              - "tablespace_name"
      oracledb.transactions.limit:
        description: "OracledbTransactionsLimitMetricConfig provides config for the oracledb.transactions.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.transactions.usage:
        description: "OracledbTransactionsUsageMetricConfig provides config for the oracledb.transactions.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.user_commits:
        description: "OracledbUserCommitsMetricConfig provides config for the oracledb.user_commits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.user_rollbacks:
        description: "OracledbUserRollbacksMetricConfig provides config for the oracledb.user_rollbacks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  events_config:
    description: EventsConfig provides config for oracledb events.
    type: object
    properties:
      db.server.query_sample:
        description: EventConfig provides common config for a  db.server.query_sample event.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      db.server.top_query:
        description: EventConfig provides common config for a  db.server.top_query event.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for oracledb resource attributes.
    type: object
    properties:
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      oracledb.instance.name:
        description: ResourceAttributeConfig provides common config for a oracledb.instance.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for oracledb metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for oracledb logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
