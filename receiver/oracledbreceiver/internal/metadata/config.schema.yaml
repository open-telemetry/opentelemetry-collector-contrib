# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for oracledb metrics.
    type: object
    properties:
      oracledb.consistent_gets:
        description: "OracledbConsistentGetsConfig provides config for the oracledb.consistent_gets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.cpu_time:
        description: "OracledbCPUTimeConfig provides config for the oracledb.cpu_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.db_block_gets:
        description: "OracledbDbBlockGetsConfig provides config for the oracledb.db_block_gets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.ddl_statements_parallelized:
        description: "OracledbDdlStatementsParallelizedConfig provides config for the oracledb.ddl_statements_parallelized metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.dml_locks.limit:
        description: "OracledbDmlLocksLimitConfig provides config for the oracledb.dml_locks.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.dml_locks.usage:
        description: "OracledbDmlLocksUsageConfig provides config for the oracledb.dml_locks.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.dml_statements_parallelized:
        description: "OracledbDmlStatementsParallelizedConfig provides config for the oracledb.dml_statements_parallelized metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.enqueue_deadlocks:
        description: "OracledbEnqueueDeadlocksConfig provides config for the oracledb.enqueue_deadlocks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_locks.limit:
        description: "OracledbEnqueueLocksLimitConfig provides config for the oracledb.enqueue_locks.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_locks.usage:
        description: "OracledbEnqueueLocksUsageConfig provides config for the oracledb.enqueue_locks.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_resources.limit:
        description: "OracledbEnqueueResourcesLimitConfig provides config for the oracledb.enqueue_resources.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.enqueue_resources.usage:
        description: "OracledbEnqueueResourcesUsageConfig provides config for the oracledb.enqueue_resources.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.exchange_deadlocks:
        description: "OracledbExchangeDeadlocksConfig provides config for the oracledb.exchange_deadlocks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.executions:
        description: "OracledbExecutionsConfig provides config for the oracledb.executions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.hard_parses:
        description: "OracledbHardParsesConfig provides config for the oracledb.hard_parses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.logical_reads:
        description: "OracledbLogicalReadsConfig provides config for the oracledb.logical_reads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.logons:
        description: "OracledbLogonsConfig provides config for the oracledb.logons metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_1_to_25_pct:
        description: "OracledbParallelOperationsDowngraded1To25PctConfig provides config for the oracledb.parallel_operations_downgraded_1_to_25_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_25_to_50_pct:
        description: "OracledbParallelOperationsDowngraded25To50PctConfig provides config for the oracledb.parallel_operations_downgraded_25_to_50_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_50_to_75_pct:
        description: "OracledbParallelOperationsDowngraded50To75PctConfig provides config for the oracledb.parallel_operations_downgraded_50_to_75_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_75_to_99_pct:
        description: "OracledbParallelOperationsDowngraded75To99PctConfig provides config for the oracledb.parallel_operations_downgraded_75_to_99_pct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_downgraded_to_serial:
        description: "OracledbParallelOperationsDowngradedToSerialConfig provides config for the oracledb.parallel_operations_downgraded_to_serial metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parallel_operations_not_downgraded:
        description: "OracledbParallelOperationsNotDowngradedConfig provides config for the oracledb.parallel_operations_not_downgraded metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.parse_calls:
        description: "OracledbParseCallsConfig provides config for the oracledb.parse_calls metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.pga_memory:
        description: "OracledbPgaMemoryConfig provides config for the oracledb.pga_memory metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.physical_read_io_requests:
        description: "OracledbPhysicalReadIoRequestsConfig provides config for the oracledb.physical_read_io_requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_reads:
        description: "OracledbPhysicalReadsConfig provides config for the oracledb.physical_reads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.physical_reads_direct:
        description: "OracledbPhysicalReadsDirectConfig provides config for the oracledb.physical_reads_direct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_write_io_requests:
        description: "OracledbPhysicalWriteIoRequestsConfig provides config for the oracledb.physical_write_io_requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_writes:
        description: "OracledbPhysicalWritesConfig provides config for the oracledb.physical_writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.physical_writes_direct:
        description: "OracledbPhysicalWritesDirectConfig provides config for the oracledb.physical_writes_direct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.processes.limit:
        description: "OracledbProcessesLimitConfig provides config for the oracledb.processes.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.processes.usage:
        description: "OracledbProcessesUsageConfig provides config for the oracledb.processes.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.queries_parallelized:
        description: "OracledbQueriesParallelizedConfig provides config for the oracledb.queries_parallelized metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      oracledb.sessions.limit:
        description: "OracledbSessionsLimitConfig provides config for the oracledb.sessions.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.sessions.usage:
        description: "OracledbSessionsUsageConfig provides config for the oracledb.sessions.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "session_type"
                - "session_status"
            default:
              - "session_type"
              - "session_status"
      oracledb.tablespace_size.limit:
        description: "OracledbTablespaceSizeLimitConfig provides config for the oracledb.tablespace_size.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "tablespace_name"
            default:
              - "tablespace_name"
      oracledb.tablespace_size.usage:
        description: "OracledbTablespaceSizeUsageConfig provides config for the oracledb.tablespace_size.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "tablespace_name"
            default:
              - "tablespace_name"
      oracledb.transactions.limit:
        description: "OracledbTransactionsLimitConfig provides config for the oracledb.transactions.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.transactions.usage:
        description: "OracledbTransactionsUsageConfig provides config for the oracledb.transactions.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.user_commits:
        description: "OracledbUserCommitsConfig provides config for the oracledb.user_commits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      oracledb.user_rollbacks:
        description: "OracledbUserRollbacksConfig provides config for the oracledb.user_rollbacks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  events_config:
    description: EventsConfig provides config for oracledb events.
    type: object
    properties:
      db.server.query_sample:
        description: EventConfig provides common config for a  db.server.query_sample event.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      db.server.top_query:
        description: EventConfig provides common config for a  db.server.top_query event.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for oracledb resource attributes.
    type: object
    properties:
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      oracledb.instance.name:
        description: ResourceAttributeConfig provides common config for a oracledb.instance.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for oracledb metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for oracledb logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
