# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for splunkenterprise metrics.
    type: object
    properties:
      splunk.aggregation.queue.ratio:
        description: "SplunkAggregationQueueRatioConfig provides config for the splunk.aggregation.queue.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.buckets.searchable.status:
        description: "SplunkBucketsSearchableStatusConfig provides config for the splunk.buckets.searchable.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.indexer.searchable"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.indexer.searchable"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.bucket.count:
        description: "SplunkDataIndexesExtendedBucketCountConfig provides config for the splunk.data.indexes.extended.bucket.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.bucket.event.count:
        description: "SplunkDataIndexesExtendedBucketEventCountConfig provides config for the splunk.data.indexes.extended.bucket.event.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.bucket.dir"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.bucket.dir"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.bucket.hot.count:
        description: "SplunkDataIndexesExtendedBucketHotCountConfig provides config for the splunk.data.indexes.extended.bucket.hot.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.bucket.dir"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.bucket.dir"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.bucket.warm.count:
        description: "SplunkDataIndexesExtendedBucketWarmCountConfig provides config for the splunk.data.indexes.extended.bucket.warm.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.bucket.dir"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.bucket.dir"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.event.count:
        description: "SplunkDataIndexesExtendedEventCountConfig provides config for the splunk.data.indexes.extended.event.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.raw.size:
        description: "SplunkDataIndexesExtendedRawSizeConfig provides config for the splunk.data.indexes.extended.raw.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.data.indexes.extended.total.size:
        description: "SplunkDataIndexesExtendedTotalSizeConfig provides config for the splunk.data.indexes.extended.total.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.health:
        description: "SplunkHealthConfig provides config for the splunk.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.feature"
                - "splunk.feature.health"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.feature"
              - "splunk.feature.health"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexer.avg.rate:
        description: "SplunkIndexerAvgRateConfig provides config for the splunk.indexer.avg.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexer.cpu.time:
        description: "SplunkIndexerCPUTimeConfig provides config for the splunk.indexer.cpu.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexer.queue.ratio:
        description: "SplunkIndexerQueueRatioConfig provides config for the splunk.indexer.queue.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexer.raw.write.time:
        description: "SplunkIndexerRawWriteTimeConfig provides config for the splunk.indexer.raw.write.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexer.rollingrestart.status:
        description: "SplunkIndexerRollingrestartStatusConfig provides config for the splunk.indexer.rollingrestart.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.searchable.restart"
                - "splunk.rollingorrestart"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.searchable.restart"
              - "splunk.rollingorrestart"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexer.throughput:
        description: "SplunkIndexerThroughputConfig provides config for the splunk.indexer.throughput metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.indexer.status"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.indexer.status"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexes.avg.size:
        description: "SplunkIndexesAvgSizeConfig provides config for the splunk.indexes.avg.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexes.avg.usage:
        description: "SplunkIndexesAvgUsageConfig provides config for the splunk.indexes.avg.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexes.bucket.count:
        description: "SplunkIndexesBucketCountConfig provides config for the splunk.indexes.bucket.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexes.median.data.age:
        description: "SplunkIndexesMedianDataAgeConfig provides config for the splunk.indexes.median.data.age metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.indexes.size:
        description: "SplunkIndexesSizeConfig provides config for the splunk.indexes.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.io.avg.iops:
        description: "SplunkIoAvgIopsConfig provides config for the splunk.io.avg.iops metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.kvstore.backup.status:
        description: "SplunkKvstoreBackupStatusConfig provides config for the splunk.kvstore.backup.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.kvstore.status.value"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.kvstore.status.value"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.kvstore.replication.status:
        description: "SplunkKvstoreReplicationStatusConfig provides config for the splunk.kvstore.replication.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.kvstore.status.value"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.kvstore.status.value"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.kvstore.status:
        description: "SplunkKvstoreStatusConfig provides config for the splunk.kvstore.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.kvstore.storage.engine"
                - "splunk.kvstore.external"
                - "splunk.kvstore.status.value"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.kvstore.storage.engine"
              - "splunk.kvstore.external"
              - "splunk.kvstore.status.value"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.license.expiration.seconds_remaining:
        description: "SplunkLicenseExpirationSecondsRemainingConfig provides config for the splunk.license.expiration.seconds_remaining metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.license.status"
                - "splunk.license.label"
                - "splunk.license.type"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.license.status"
              - "splunk.license.label"
              - "splunk.license.type"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.license.index.usage:
        description: "SplunkLicenseIndexUsageConfig provides config for the splunk.license.index.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.index.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.index.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.parse.queue.ratio:
        description: "SplunkParseQueueRatioConfig provides config for the splunk.parse.queue.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.pipeline.set.count:
        description: "SplunkPipelineSetCountConfig provides config for the splunk.pipeline.set.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.scheduler.avg.execution.latency:
        description: "SplunkSchedulerAvgExecutionLatencyConfig provides config for the splunk.scheduler.avg.execution.latency metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.scheduler.avg.run.time:
        description: "SplunkSchedulerAvgRunTimeConfig provides config for the splunk.scheduler.avg.run.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.scheduler.completion.ratio:
        description: "SplunkSchedulerCompletionRatioConfig provides config for the splunk.scheduler.completion.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.search.duration:
        description: "SplunkSearchDurationConfig provides config for the splunk.search.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.search.initiation:
        description: "SplunkSearchInitiationConfig provides config for the splunk.search.initiation metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.search.status:
        description: "SplunkSearchStatusConfig provides config for the splunk.search.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.search.state"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.search.state"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.search.success:
        description: "SplunkSearchSuccessConfig provides config for the splunk.search.success metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.introspection.queues.current:
        description: "SplunkServerIntrospectionQueuesCurrentConfig provides config for the splunk.server.introspection.queues.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.queue.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.queue.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.introspection.queues.current.bytes:
        description: "SplunkServerIntrospectionQueuesCurrentBytesConfig provides config for the splunk.server.introspection.queues.current.bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.queue.name"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.queue.name"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.adhoc:
        description: "SplunkServerSearchartifactsAdhocConfig provides config for the splunk.server.searchartifacts.adhoc metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.adhoc.size:
        description: "SplunkServerSearchartifactsAdhocSizeConfig provides config for the splunk.server.searchartifacts.adhoc.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.completed:
        description: "SplunkServerSearchartifactsCompletedConfig provides config for the splunk.server.searchartifacts.completed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.completed.size:
        description: "SplunkServerSearchartifactsCompletedSizeConfig provides config for the splunk.server.searchartifacts.completed.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.incomplete:
        description: "SplunkServerSearchartifactsIncompleteConfig provides config for the splunk.server.searchartifacts.incomplete metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.incomplete.size:
        description: "SplunkServerSearchartifactsIncompleteSizeConfig provides config for the splunk.server.searchartifacts.incomplete.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.invalid:
        description: "SplunkServerSearchartifactsInvalidConfig provides config for the splunk.server.searchartifacts.invalid metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.job.cache.count:
        description: "SplunkServerSearchartifactsJobCacheCountConfig provides config for the splunk.server.searchartifacts.job.cache.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.job.cache.size:
        description: "SplunkServerSearchartifactsJobCacheSizeConfig provides config for the splunk.server.searchartifacts.job.cache.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.searchartifacts.cache.type"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.searchartifacts.cache.type"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.savedsearches:
        description: "SplunkServerSearchartifactsSavedsearchesConfig provides config for the splunk.server.searchartifacts.savedsearches metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.scheduled:
        description: "SplunkServerSearchartifactsScheduledConfig provides config for the splunk.server.searchartifacts.scheduled metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.server.searchartifacts.scheduled.size:
        description: "SplunkServerSearchartifactsScheduledSizeConfig provides config for the splunk.server.searchartifacts.scheduled.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
      splunk.typing.queue.ratio:
        description: "SplunkTypingQueueRatioConfig provides config for the splunk.typing.queue.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "splunk.host"
                - "splunk.splunkd.build"
                - "splunk.splunkd.version"
            default:
              - "splunk.host"
              - "splunk.splunkd.build"
              - "splunk.splunkd.version"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for splunkenterprise metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
