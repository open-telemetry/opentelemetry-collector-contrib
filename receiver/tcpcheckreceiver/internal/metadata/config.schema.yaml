# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for tcpcheck metrics.
    type: object
    properties:
      tcpcheck.duration:
        description: "TcpcheckDurationConfig provides config for the tcpcheck.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      tcpcheck.error:
        description: "TcpcheckErrorConfig provides config for the tcpcheck.error metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      tcpcheck.status:
        description: "TcpcheckStatusConfig provides config for the tcpcheck.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for tcpcheck metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
