receivers:
  snmp:
    metrics:
      m3:
        gauge:
          value_type: float
        scalar_oids:
          - oid: "1"  

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [snmp]
      processors: [nop]
      exporters: [nop]
