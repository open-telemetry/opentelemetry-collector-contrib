receivers:
  snmp:
    collection_interval: 1s
    endpoint: udp://localhost:161
    version: "v3"
    security_level: "auth_priv"
    user: u
    auth_type: "md5"
    auth_password: "p"
    privacy_type: "des"
    privacy_password: "pp"
    
    metrics:
      m3:
        unit: "By"
        gauge:
          value_type: float
        scalar_oids:
          - oid: "1"  

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [snmp]
      processors: [nop]
      exporters: [nop]
