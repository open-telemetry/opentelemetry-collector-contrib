receivers:
  snmp:
    attributes:
      a2:
        enum:
          - val1
    metrics:
      m3:
        unit: "By"
        sum:
          aggregation: cumulative
          monotonic: true
          value_type: float
        column_oids:
          - oid: "1"
            attributes:
              - value: val1

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [snmp]
      processors: [nop]
      exporters: [nop]
