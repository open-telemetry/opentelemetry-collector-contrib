apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Name }}-config
  namespace: default
data:
  relay: |
    exporters:
      otlphttp:
        endpoint: http://{{ .HostEndpoint }}:4318
        tls:
          insecure: true
    extensions:
      health_check: {}
    processors:
    receivers:
      k8s_cluster:
        
    service:
      telemetry:
        logs:
          level: "debug"
      extensions:
      - health_check
      pipelines:
        metrics:
          exporters:
          - otlphttp
          receivers:
          - k8s_cluster
