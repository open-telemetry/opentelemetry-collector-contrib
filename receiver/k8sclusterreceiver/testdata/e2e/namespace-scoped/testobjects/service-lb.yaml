apiVersion: v1
kind: Service
metadata:
  name: test-service-lb
  namespace: my-namespace
  labels:
    app: test
spec:
  selector:
    app: test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
  type: LoadBalancer
  trafficDistribution: PreferClose
