receivers:
  github:
    access_token: "my_token"
    log_type: "user"
    name: "github"
    poll_interval: 60s

  github/customname:
    access_token: "my_token"
    log_type: "user"
    name: "github"
    poll_interval: 60s

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [github, github/customname]
      processors: [nop]
      exporters: [nop]
