display_name: GitHub Receiver
type: github

sem_conv_version: 1.40.0

status:
  class: receiver
  stability:
    alpha: [metrics, traces]
  distributions: [contrib]
  codeowners:
    active: [adrielp, crobert-1, TylerHelmuth]

reaggregation_enabled: true

feature_gates:
  - id: receiver.githubreceiver.UseCheckRunID
    description: >-
      When enabled, deterministic job, step, and queue span IDs are derived from the
      GitHub check_run_id (the WorkflowJob.id webhook field, also exposed in-runner as
      ${{ job.check_run_id }}). Step span IDs additionally incorporate the step's raw
      name, allowing in-action telemetry tools to reproduce matching span IDs without
      calling the GitHub REST API and without knowing the runner-dependent step
      number. Payloads missing a check_run_id are rejected while this gate is enabled.
      Workflows with duplicate step names within a job will log a warning; duplicate
      steps will share the same span ID.
    stage: beta
    from_version: "v0.151.0"
    reference_url: https://github.com/open-telemetry/opentelemetry-collector-contrib/issues/44856

resource_attributes:
  vcs.owner.name:
    enabled: true
    description: The group owner within the version control system
    type: string
  vcs.provider.name:
    enabled: true
    description: The name of the version control system provider
    type: string

attributes:
  vcs.change.state:
    description: The state of a change (pull request)
    type: string
    requirement_level: recommended
    enum:
      - open
      - merged
  vcs.line_change.type:
    description: The type of line change being measured on a ref (branch).
    type: string
    requirement_level: recommended
    enum:
      - added
      - removed
  vcs.ref.base.name:
    description: The name of the VCS base reference (branch/tag) for comparison.
    type: string
    requirement_level: recommended
  vcs.ref.base.type:
    description: The type of the base reference (branch, tag).
    type: string
    requirement_level: recommended
    enum:
      - branch
      - tag
  vcs.ref.head.name:
    description: The name of the VCS head reference (branch).
    type: string
    requirement_level: recommended
  vcs.ref.head.type:
    description: The type of the head reference (branch, tag).
    type: string
    requirement_level: recommended
    enum:
      - branch
      - tag
  vcs.ref.type:
    description: The type of the reference in the repository.
    type: string
    requirement_level: recommended
    enum:
      - branch
      - tag
  vcs.repository.name:
    description: The name of the VCS repository.
    type: string
    requirement_level: recommended
  vcs.repository.url.full:
    description: The canonical URL of the repository providing the complete HTTPS address.
    type: string
    requirement_level: recommended
  vcs.revision_delta.direction:
    description: The type of revision comparison.
    type: string
    requirement_level: recommended
    enum:
      - ahead
      - behind

metrics:
  vcs.change.count:
    description: The number of changes (pull requests) in a repository, categorized by their state (either open or merged).
    enabled: true
    stability: development
    gauge:
      value_type: int
    unit: "{change}"
    attributes: [vcs.repository.url.full, vcs.change.state, vcs.repository.name]
  vcs.change.duration:
    enabled: true
    description: The time duration a change (pull request/merge request/changelist) has been in an open state.
    stability: development
    unit: s
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.head.name, vcs.change.state]
  vcs.change.time_to_approval:
    enabled: true
    description: The amount of time it took a change (pull request) to go from open to approved.
    stability: development
    unit: s
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.head.name]
  vcs.change.time_to_merge:
    enabled: true
    description: The amount of time it took a change (pull request) to go from open to merged.
    stability: development
    unit: s
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.head.name]
  vcs.contributor.count:
    enabled: false
    description: The number of unique contributors to a repository.
    stability: development
    unit: "{contributor}"
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name]
  vcs.ref.count:
    enabled: true
    description: The number of refs of type branch in a repository.
    stability: development
    unit: "{ref}"
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.type]
  vcs.ref.lines_delta:
    enabled: true
    description: The number of lines added/removed in a ref (branch) relative to the default branch (trunk).
    stability: development
    unit: "{line}"
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.head.name, vcs.ref.head.type, vcs.ref.base.name, vcs.ref.base.type, vcs.line_change.type]
  vcs.ref.revisions_delta:
    enabled: true
    description: The number of revisions (commits) a ref (branch) is ahead/behind the branch from trunk (default).
    stability: development
    unit: "{revision}"
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.head.name, vcs.ref.head.type, vcs.ref.base.name, vcs.ref.base.type, vcs.revision_delta.direction]
  vcs.ref.time:
    enabled: true
    description: Time a ref (branch) created from the default branch (trunk) has existed. The `vcs.ref.type` attribute will always be `branch`.
    stability: development
    unit: s
    gauge:
      value_type: int
    attributes: [vcs.repository.url.full, vcs.repository.name, vcs.ref.head.name, vcs.ref.head.type]
  vcs.repository.count:
    enabled: true
    description: The number of repositories in an organization.
    stability: development
    unit: "{repository}"
    gauge:
      value_type: int
    attributes: []

tests:
  config:
    webhook:
      endpoint: "localhost:8080"
