type: tcplog

status:
  class: receiver
  stability:
    alpha: [logs]
  distributions: [contrib, splunk, observiq, sumo]
  codeowners:
    active: [djaglowski]

tests:
  config:
    listen_address: "0.0.0.0:0"