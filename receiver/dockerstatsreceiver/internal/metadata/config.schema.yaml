# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for docker_stats metrics.
    type: object
    properties:
      container.blockio.io_merged_recursive:
        description: "ContainerBlockioIoMergedRecursiveConfig provides config for the container.blockio.io_merged_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.blockio.io_queued_recursive:
        description: "ContainerBlockioIoQueuedRecursiveConfig provides config for the container.blockio.io_queued_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.blockio.io_service_bytes_recursive:
        description: "ContainerBlockioIoServiceBytesRecursiveConfig provides config for the container.blockio.io_service_bytes_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.blockio.io_service_time_recursive:
        description: "ContainerBlockioIoServiceTimeRecursiveConfig provides config for the container.blockio.io_service_time_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.blockio.io_serviced_recursive:
        description: "ContainerBlockioIoServicedRecursiveConfig provides config for the container.blockio.io_serviced_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.blockio.io_time_recursive:
        description: "ContainerBlockioIoTimeRecursiveConfig provides config for the container.blockio.io_time_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.blockio.io_wait_time_recursive:
        description: "ContainerBlockioIoWaitTimeRecursiveConfig provides config for the container.blockio.io_wait_time_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.blockio.sectors_recursive:
        description: "ContainerBlockioSectorsRecursiveConfig provides config for the container.blockio.sectors_recursive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.limit:
        description: "ContainerCPULimitConfig provides config for the container.cpu.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.logical.count:
        description: "ContainerCPULogicalCountConfig provides config for the container.cpu.logical.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.shares:
        description: "ContainerCPUSharesConfig provides config for the container.cpu.shares metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.throttling_data.periods:
        description: "ContainerCPUThrottlingDataPeriodsConfig provides config for the container.cpu.throttling_data.periods metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.throttling_data.throttled_periods:
        description: "ContainerCPUThrottlingDataThrottledPeriodsConfig provides config for the container.cpu.throttling_data.throttled_periods metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.throttling_data.throttled_time:
        description: "ContainerCPUThrottlingDataThrottledTimeConfig provides config for the container.cpu.throttling_data.throttled_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.usage.kernelmode:
        description: "ContainerCPUUsageKernelmodeConfig provides config for the container.cpu.usage.kernelmode metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.cpu.usage.percpu:
        description: "ContainerCPUUsagePercpuConfig provides config for the container.cpu.usage.percpu metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.usage.system:
        description: "ContainerCPUUsageSystemConfig provides config for the container.cpu.usage.system metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.cpu.usage.total:
        description: "ContainerCPUUsageTotalConfig provides config for the container.cpu.usage.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.cpu.usage.usermode:
        description: "ContainerCPUUsageUsermodeConfig provides config for the container.cpu.usage.usermode metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.cpu.utilization:
        description: "ContainerCPUUtilizationConfig provides config for the container.cpu.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.memory.active_anon:
        description: "ContainerMemoryActiveAnonConfig provides config for the container.memory.active_anon metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.active_file:
        description: "ContainerMemoryActiveFileConfig provides config for the container.memory.active_file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.anon:
        description: "ContainerMemoryAnonConfig provides config for the container.memory.anon metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.cache:
        description: "ContainerMemoryCacheConfig provides config for the container.memory.cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.dirty:
        description: "ContainerMemoryDirtyConfig provides config for the container.memory.dirty metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.fails:
        description: "ContainerMemoryFailsConfig provides config for the container.memory.fails metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.file:
        description: "ContainerMemoryFileConfig provides config for the container.memory.file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.memory.hierarchical_memory_limit:
        description: "ContainerMemoryHierarchicalMemoryLimitConfig provides config for the container.memory.hierarchical_memory_limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.hierarchical_memsw_limit:
        description: "ContainerMemoryHierarchicalMemswLimitConfig provides config for the container.memory.hierarchical_memsw_limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.inactive_anon:
        description: "ContainerMemoryInactiveAnonConfig provides config for the container.memory.inactive_anon metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.inactive_file:
        description: "ContainerMemoryInactiveFileConfig provides config for the container.memory.inactive_file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.mapped_file:
        description: "ContainerMemoryMappedFileConfig provides config for the container.memory.mapped_file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.percent:
        description: "ContainerMemoryPercentConfig provides config for the container.memory.percent metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.memory.pgfault:
        description: "ContainerMemoryPgfaultConfig provides config for the container.memory.pgfault metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.pgmajfault:
        description: "ContainerMemoryPgmajfaultConfig provides config for the container.memory.pgmajfault metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.pgpgin:
        description: "ContainerMemoryPgpginConfig provides config for the container.memory.pgpgin metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.pgpgout:
        description: "ContainerMemoryPgpgoutConfig provides config for the container.memory.pgpgout metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.rss:
        description: "ContainerMemoryRssConfig provides config for the container.memory.rss metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.rss_huge:
        description: "ContainerMemoryRssHugeConfig provides config for the container.memory.rss_huge metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_active_anon:
        description: "ContainerMemoryTotalActiveAnonConfig provides config for the container.memory.total_active_anon metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_active_file:
        description: "ContainerMemoryTotalActiveFileConfig provides config for the container.memory.total_active_file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_cache:
        description: "ContainerMemoryTotalCacheConfig provides config for the container.memory.total_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.memory.total_dirty:
        description: "ContainerMemoryTotalDirtyConfig provides config for the container.memory.total_dirty metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_inactive_anon:
        description: "ContainerMemoryTotalInactiveAnonConfig provides config for the container.memory.total_inactive_anon metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_inactive_file:
        description: "ContainerMemoryTotalInactiveFileConfig provides config for the container.memory.total_inactive_file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_mapped_file:
        description: "ContainerMemoryTotalMappedFileConfig provides config for the container.memory.total_mapped_file metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_pgfault:
        description: "ContainerMemoryTotalPgfaultConfig provides config for the container.memory.total_pgfault metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_pgmajfault:
        description: "ContainerMemoryTotalPgmajfaultConfig provides config for the container.memory.total_pgmajfault metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_pgpgin:
        description: "ContainerMemoryTotalPgpginConfig provides config for the container.memory.total_pgpgin metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_pgpgout:
        description: "ContainerMemoryTotalPgpgoutConfig provides config for the container.memory.total_pgpgout metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_rss:
        description: "ContainerMemoryTotalRssConfig provides config for the container.memory.total_rss metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_rss_huge:
        description: "ContainerMemoryTotalRssHugeConfig provides config for the container.memory.total_rss_huge metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_unevictable:
        description: "ContainerMemoryTotalUnevictableConfig provides config for the container.memory.total_unevictable metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.total_writeback:
        description: "ContainerMemoryTotalWritebackConfig provides config for the container.memory.total_writeback metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.unevictable:
        description: "ContainerMemoryUnevictableConfig provides config for the container.memory.unevictable metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.usage.limit:
        description: "ContainerMemoryUsageLimitConfig provides config for the container.memory.usage.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.memory.usage.max:
        description: "ContainerMemoryUsageMaxConfig provides config for the container.memory.usage.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.memory.usage.total:
        description: "ContainerMemoryUsageTotalConfig provides config for the container.memory.usage.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.memory.writeback:
        description: "ContainerMemoryWritebackConfig provides config for the container.memory.writeback metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.network.io.usage.rx_bytes:
        description: "ContainerNetworkIoUsageRxBytesConfig provides config for the container.network.io.usage.rx_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.network.io.usage.rx_dropped:
        description: "ContainerNetworkIoUsageRxDroppedConfig provides config for the container.network.io.usage.rx_dropped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.network.io.usage.rx_errors:
        description: "ContainerNetworkIoUsageRxErrorsConfig provides config for the container.network.io.usage.rx_errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.network.io.usage.rx_packets:
        description: "ContainerNetworkIoUsageRxPacketsConfig provides config for the container.network.io.usage.rx_packets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.network.io.usage.tx_bytes:
        description: "ContainerNetworkIoUsageTxBytesConfig provides config for the container.network.io.usage.tx_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.network.io.usage.tx_dropped:
        description: "ContainerNetworkIoUsageTxDroppedConfig provides config for the container.network.io.usage.tx_dropped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.network.io.usage.tx_errors:
        description: "ContainerNetworkIoUsageTxErrorsConfig provides config for the container.network.io.usage.tx_errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.network.io.usage.tx_packets:
        description: "ContainerNetworkIoUsageTxPacketsConfig provides config for the container.network.io.usage.tx_packets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.pids.count:
        description: "ContainerPidsCountConfig provides config for the container.pids.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.pids.limit:
        description: "ContainerPidsLimitConfig provides config for the container.pids.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.restarts:
        description: "ContainerRestartsConfig provides config for the container.restarts metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.uptime:
        description: "ContainerUptimeConfig provides config for the container.uptime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for docker_stats resource attributes.
    type: object
    properties:
      container.command_line:
        description: ResourceAttributeConfig provides common config for a container.command_line resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      container.hostname:
        description: ResourceAttributeConfig provides common config for a container.hostname resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      container.id:
        description: ResourceAttributeConfig provides common config for a container.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      container.image.id:
        description: ResourceAttributeConfig provides common config for a container.image.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      container.image.name:
        description: ResourceAttributeConfig provides common config for a container.image.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      container.name:
        description: ResourceAttributeConfig provides common config for a container.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      container.runtime:
        description: ResourceAttributeConfig provides common config for a container.runtime resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for docker_stats metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
