$defs:
  attribute_buffer_pool_data:
    description: AttributeBufferPoolData specifies the value buffer_pool_data attribute.
    type: integer
  attribute_buffer_pool_operations:
    description: AttributeBufferPoolOperations specifies the value buffer_pool_operations attribute.
    type: integer
  attribute_buffer_pool_pages:
    description: AttributeBufferPoolPages specifies the value buffer_pool_pages attribute.
    type: integer
  attribute_cache_status:
    description: AttributeCacheStatus specifies the value cache_status attribute.
    type: integer
  attribute_command:
    description: AttributeCommand specifies the value command attribute.
    type: integer
  attribute_connection_error:
    description: AttributeConnectionError specifies the value connection_error attribute.
    type: integer
  attribute_connection_status:
    description: AttributeConnectionStatus specifies the value connection_status attribute.
    type: integer
  attribute_db_system_name:
    description: AttributeDbSystemName specifies the value db.system.name attribute.
    type: integer
  attribute_direction:
    description: AttributeDirection specifies the value direction attribute.
    type: integer
  attribute_disk_io_direction:
    description: AttributeDiskIoDirection specifies the value disk.io.direction attribute.
    type: integer
  attribute_double_writes:
    description: AttributeDoubleWrites specifies the value double_writes attribute.
    type: integer
  attribute_event_state:
    description: AttributeEventState specifies the value event_state attribute.
    type: integer
  attribute_handler:
    description: AttributeHandler specifies the value handler attribute.
    type: integer
  attribute_io_waits_operations:
    description: AttributeIoWaitsOperations specifies the value io_waits_operations attribute.
    type: integer
  attribute_join_kind:
    description: AttributeJoinKind specifies the value join_kind attribute.
    type: integer
  attribute_locks:
    description: AttributeLocks specifies the value locks attribute.
    type: integer
  attribute_log_operations:
    description: AttributeLogOperations specifies the value log_operations attribute.
    type: integer
  attribute_mysql_replica_thread_type:
    description: AttributeMysqlReplicaThreadType specifies the value mysql.replica.thread.type attribute.
    type: integer
  attribute_mysqlx_threads:
    description: AttributeMysqlxThreads specifies the value mysqlx_threads attribute.
    type: integer
  attribute_opened_resources:
    description: AttributeOpenedResources specifies the value opened_resources attribute.
    type: integer
  attribute_operations:
    description: AttributeOperations specifies the value operations attribute.
    type: integer
  attribute_page_operations:
    description: AttributePageOperations specifies the value page_operations attribute.
    type: integer
  attribute_prepared_statements_command:
    description: AttributePreparedStatementsCommand specifies the value prepared_statements_command attribute.
    type: integer
  attribute_read_lock_type:
    description: AttributeReadLockType specifies the value read_lock_type attribute.
    type: integer
  attribute_row_locks:
    description: AttributeRowLocks specifies the value row_locks attribute.
    type: integer
  attribute_row_operations:
    description: AttributeRowOperations specifies the value row_operations attribute.
    type: integer
  attribute_sorts:
    description: AttributeSorts specifies the value sorts attribute.
    type: integer
  attribute_table_size_type:
    description: AttributeTableSizeType specifies the value table_size_type attribute.
    type: integer
  attribute_threads:
    description: AttributeThreads specifies the value threads attribute.
    type: integer
  attribute_tmp_resource:
    description: AttributeTmpResource specifies the value tmp_resource attribute.
    type: integer
  attribute_write_lock_type:
    description: AttributeWriteLockType specifies the value write_lock_type attribute.
    type: integer
  db_system_name_resource_attribute_config:
    description: DbSystemNameResourceAttributeConfig provides config for the db.system.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  db_system_version_resource_attribute_config:
    description: DbSystemVersionResourceAttributeConfig provides config for the db.system.version resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  event_config:
    description: EventConfig provides common config for a particular event.
    type: object
    properties:
      enabled:
        type: boolean
  events_config:
    description: EventsConfig provides config for mysql events.
    type: object
    properties:
      db.server.query_sample:
        $ref: event_config
      db.server.top_query:
        $ref: event_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for mysql logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for mysql metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  metrics_config:
    description: MetricsConfig provides config for mysql metrics.
    type: object
    properties:
      mysql.buffer_pool.data_pages:
        $ref: mysql_buffer_pool_data_pages_metric_config
      mysql.buffer_pool.limit:
        $ref: mysql_buffer_pool_limit_metric_config
      mysql.buffer_pool.operations:
        $ref: mysql_buffer_pool_operations_metric_config
      mysql.buffer_pool.page_flushes:
        $ref: mysql_buffer_pool_page_flushes_metric_config
      mysql.buffer_pool.pages:
        $ref: mysql_buffer_pool_pages_metric_config
      mysql.buffer_pool.usage:
        $ref: mysql_buffer_pool_usage_metric_config
      mysql.client.network.io:
        $ref: mysql_client_network_io_metric_config
      mysql.commands:
        $ref: mysql_commands_metric_config
      mysql.connection.count:
        $ref: mysql_connection_count_metric_config
      mysql.connection.errors:
        $ref: mysql_connection_errors_metric_config
      mysql.double_writes:
        $ref: mysql_double_writes_metric_config
      mysql.file.open:
        $ref: mysql_file_open_metric_config
      mysql.handlers:
        $ref: mysql_handlers_metric_config
      mysql.index.io.wait.count:
        $ref: mysql_index_io_wait_count_metric_config
      mysql.index.io.wait.time:
        $ref: mysql_index_io_wait_time_metric_config
      mysql.innodb.data_file.io:
        $ref: mysql_innodb_data_file_io_metric_config
      mysql.innodb.operation.pending:
        $ref: mysql_innodb_operation_pending_metric_config
      mysql.innodb.row_lock.wait.count:
        $ref: mysql_innodb_row_lock_wait_count_metric_config
      mysql.innodb.row_lock.wait.duration.avg:
        $ref: mysql_innodb_row_lock_wait_duration_avg_metric_config
      mysql.innodb.row_lock.wait.duration.max:
        $ref: mysql_innodb_row_lock_wait_duration_max_metric_config
      mysql.joins:
        $ref: mysql_joins_metric_config
      mysql.locks:
        $ref: mysql_locks_metric_config
      mysql.log_operations:
        $ref: mysql_log_operations_metric_config
      mysql.max_used_connections:
        $ref: mysql_max_used_connections_metric_config
      mysql.mysqlx_connections:
        $ref: mysql_mysqlx_connections_metric_config
      mysql.mysqlx_worker_threads:
        $ref: mysql_mysqlx_worker_threads_metric_config
      mysql.opened_resources:
        $ref: mysql_opened_resources_metric_config
      mysql.operations:
        $ref: mysql_operations_metric_config
      mysql.page_operations:
        $ref: mysql_page_operations_metric_config
      mysql.page_size:
        $ref: mysql_page_size_metric_config
      mysql.prepared_statements:
        $ref: mysql_prepared_statements_metric_config
      mysql.query.client.count:
        $ref: mysql_query_client_count_metric_config
      mysql.query.count:
        $ref: mysql_query_count_metric_config
      mysql.query.slow.count:
        $ref: mysql_query_slow_count_metric_config
      mysql.replica.sql_delay:
        $ref: mysql_replica_sql_delay_metric_config
      mysql.replica.temp_table.open:
        $ref: mysql_replica_temp_table_open_metric_config
      mysql.replica.thread.running:
        $ref: mysql_replica_thread_running_metric_config
      mysql.replica.time_behind_source:
        $ref: mysql_replica_time_behind_source_metric_config
      mysql.row_locks:
        $ref: mysql_row_locks_metric_config
      mysql.row_operations:
        $ref: mysql_row_operations_metric_config
      mysql.sorts:
        $ref: mysql_sorts_metric_config
      mysql.statement_event.count:
        $ref: mysql_statement_event_count_metric_config
      mysql.statement_event.wait.time:
        $ref: mysql_statement_event_wait_time_metric_config
      mysql.table.average_row_length:
        $ref: mysql_table_average_row_length_metric_config
      mysql.table.io.wait.count:
        $ref: mysql_table_io_wait_count_metric_config
      mysql.table.io.wait.time:
        $ref: mysql_table_io_wait_time_metric_config
      mysql.table.lock_wait.read.count:
        $ref: mysql_table_lock_wait_read_count_metric_config
      mysql.table.lock_wait.read.time:
        $ref: mysql_table_lock_wait_read_time_metric_config
      mysql.table.lock_wait.write.count:
        $ref: mysql_table_lock_wait_write_count_metric_config
      mysql.table.lock_wait.write.time:
        $ref: mysql_table_lock_wait_write_time_metric_config
      mysql.table.open:
        $ref: mysql_table_open_metric_config
      mysql.table.rows:
        $ref: mysql_table_rows_metric_config
      mysql.table.size:
        $ref: mysql_table_size_metric_config
      mysql.table_open_cache:
        $ref: mysql_table_open_cache_metric_config
      mysql.thread.slow_launch:
        $ref: mysql_thread_slow_launch_metric_config
      mysql.threads:
        $ref: mysql_threads_metric_config
      mysql.tmp_resources:
        $ref: mysql_tmp_resources_metric_config
      mysql.uptime:
        $ref: mysql_uptime_metric_config
  mysql_buffer_pool_data_pages_metric_attribute_key:
    description: MysqlBufferPoolDataPagesMetricAttributeKey specifies the key of an attribute for the mysql.buffer_pool.data_pages metric.
    type: string
  mysql_buffer_pool_data_pages_metric_config:
    description: MysqlBufferPoolDataPagesMetricConfig provides config for the mysql.buffer_pool.data_pages metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_buffer_pool_data_pages_metric_attribute_key
      enabled:
        type: boolean
  mysql_buffer_pool_limit_metric_config:
    description: MysqlBufferPoolLimitMetricConfig provides config for the mysql.buffer_pool.limit metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_buffer_pool_operations_metric_attribute_key:
    description: MysqlBufferPoolOperationsMetricAttributeKey specifies the key of an attribute for the mysql.buffer_pool.operations metric.
    type: string
  mysql_buffer_pool_operations_metric_config:
    description: MysqlBufferPoolOperationsMetricConfig provides config for the mysql.buffer_pool.operations metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_buffer_pool_operations_metric_attribute_key
      enabled:
        type: boolean
  mysql_buffer_pool_page_flushes_metric_config:
    description: MysqlBufferPoolPageFlushesMetricConfig provides config for the mysql.buffer_pool.page_flushes metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_buffer_pool_pages_metric_attribute_key:
    description: MysqlBufferPoolPagesMetricAttributeKey specifies the key of an attribute for the mysql.buffer_pool.pages metric.
    type: string
  mysql_buffer_pool_pages_metric_config:
    description: MysqlBufferPoolPagesMetricConfig provides config for the mysql.buffer_pool.pages metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_buffer_pool_pages_metric_attribute_key
      enabled:
        type: boolean
  mysql_buffer_pool_usage_metric_attribute_key:
    description: MysqlBufferPoolUsageMetricAttributeKey specifies the key of an attribute for the mysql.buffer_pool.usage metric.
    type: string
  mysql_buffer_pool_usage_metric_config:
    description: MysqlBufferPoolUsageMetricConfig provides config for the mysql.buffer_pool.usage metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_buffer_pool_usage_metric_attribute_key
      enabled:
        type: boolean
  mysql_client_network_io_metric_attribute_key:
    description: MysqlClientNetworkIoMetricAttributeKey specifies the key of an attribute for the mysql.client.network.io metric.
    type: string
  mysql_client_network_io_metric_config:
    description: MysqlClientNetworkIoMetricConfig provides config for the mysql.client.network.io metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_client_network_io_metric_attribute_key
      enabled:
        type: boolean
  mysql_commands_metric_attribute_key:
    description: MysqlCommandsMetricAttributeKey specifies the key of an attribute for the mysql.commands metric.
    type: string
  mysql_commands_metric_config:
    description: MysqlCommandsMetricConfig provides config for the mysql.commands metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_commands_metric_attribute_key
      enabled:
        type: boolean
  mysql_connection_count_metric_config:
    description: MysqlConnectionCountMetricConfig provides config for the mysql.connection.count metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_connection_errors_metric_attribute_key:
    description: MysqlConnectionErrorsMetricAttributeKey specifies the key of an attribute for the mysql.connection.errors metric.
    type: string
  mysql_connection_errors_metric_config:
    description: MysqlConnectionErrorsMetricConfig provides config for the mysql.connection.errors metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_connection_errors_metric_attribute_key
      enabled:
        type: boolean
  mysql_double_writes_metric_attribute_key:
    description: MysqlDoubleWritesMetricAttributeKey specifies the key of an attribute for the mysql.double_writes metric.
    type: string
  mysql_double_writes_metric_config:
    description: MysqlDoubleWritesMetricConfig provides config for the mysql.double_writes metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_double_writes_metric_attribute_key
      enabled:
        type: boolean
  mysql_file_open_metric_config:
    description: MysqlFileOpenMetricConfig provides config for the mysql.file.open metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_handlers_metric_attribute_key:
    description: MysqlHandlersMetricAttributeKey specifies the key of an attribute for the mysql.handlers metric.
    type: string
  mysql_handlers_metric_config:
    description: MysqlHandlersMetricConfig provides config for the mysql.handlers metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_handlers_metric_attribute_key
      enabled:
        type: boolean
  mysql_index_io_wait_count_metric_attribute_key:
    description: MysqlIndexIoWaitCountMetricAttributeKey specifies the key of an attribute for the mysql.index.io.wait.count metric.
    type: string
  mysql_index_io_wait_count_metric_config:
    description: MysqlIndexIoWaitCountMetricConfig provides config for the mysql.index.io.wait.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_index_io_wait_count_metric_attribute_key
      enabled:
        type: boolean
  mysql_index_io_wait_time_metric_attribute_key:
    description: MysqlIndexIoWaitTimeMetricAttributeKey specifies the key of an attribute for the mysql.index.io.wait.time metric.
    type: string
  mysql_index_io_wait_time_metric_config:
    description: MysqlIndexIoWaitTimeMetricConfig provides config for the mysql.index.io.wait.time metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_index_io_wait_time_metric_attribute_key
      enabled:
        type: boolean
  mysql_innodb_data_file_io_metric_attribute_key:
    description: MysqlInnodbDataFileIoMetricAttributeKey specifies the key of an attribute for the mysql.innodb.data_file.io metric.
    type: string
  mysql_innodb_data_file_io_metric_config:
    description: MysqlInnodbDataFileIoMetricConfig provides config for the mysql.innodb.data_file.io metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_innodb_data_file_io_metric_attribute_key
      enabled:
        type: boolean
  mysql_innodb_operation_pending_metric_attribute_key:
    description: MysqlInnodbOperationPendingMetricAttributeKey specifies the key of an attribute for the mysql.innodb.operation.pending metric.
    type: string
  mysql_innodb_operation_pending_metric_config:
    description: MysqlInnodbOperationPendingMetricConfig provides config for the mysql.innodb.operation.pending metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_innodb_operation_pending_metric_attribute_key
      enabled:
        type: boolean
  mysql_innodb_row_lock_wait_count_metric_config:
    description: MysqlInnodbRowLockWaitCountMetricConfig provides config for the mysql.innodb.row_lock.wait.count metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_innodb_row_lock_wait_duration_avg_metric_config:
    description: MysqlInnodbRowLockWaitDurationAvgMetricConfig provides config for the mysql.innodb.row_lock.wait.duration.avg metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_innodb_row_lock_wait_duration_max_metric_config:
    description: MysqlInnodbRowLockWaitDurationMaxMetricConfig provides config for the mysql.innodb.row_lock.wait.duration.max metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_instance_endpoint_resource_attribute_config:
    description: MysqlInstanceEndpointResourceAttributeConfig provides config for the mysql.instance.endpoint resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  mysql_joins_metric_attribute_key:
    description: MysqlJoinsMetricAttributeKey specifies the key of an attribute for the mysql.joins metric.
    type: string
  mysql_joins_metric_config:
    description: MysqlJoinsMetricConfig provides config for the mysql.joins metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_joins_metric_attribute_key
      enabled:
        type: boolean
  mysql_locks_metric_attribute_key:
    description: MysqlLocksMetricAttributeKey specifies the key of an attribute for the mysql.locks metric.
    type: string
  mysql_locks_metric_config:
    description: MysqlLocksMetricConfig provides config for the mysql.locks metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_locks_metric_attribute_key
      enabled:
        type: boolean
  mysql_log_operations_metric_attribute_key:
    description: MysqlLogOperationsMetricAttributeKey specifies the key of an attribute for the mysql.log_operations metric.
    type: string
  mysql_log_operations_metric_config:
    description: MysqlLogOperationsMetricConfig provides config for the mysql.log_operations metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_log_operations_metric_attribute_key
      enabled:
        type: boolean
  mysql_max_used_connections_metric_config:
    description: MysqlMaxUsedConnectionsMetricConfig provides config for the mysql.max_used_connections metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_mysqlx_connections_metric_attribute_key:
    description: MysqlMysqlxConnectionsMetricAttributeKey specifies the key of an attribute for the mysql.mysqlx_connections metric.
    type: string
  mysql_mysqlx_connections_metric_config:
    description: MysqlMysqlxConnectionsMetricConfig provides config for the mysql.mysqlx_connections metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_mysqlx_connections_metric_attribute_key
      enabled:
        type: boolean
  mysql_mysqlx_worker_threads_metric_attribute_key:
    description: MysqlMysqlxWorkerThreadsMetricAttributeKey specifies the key of an attribute for the mysql.mysqlx_worker_threads metric.
    type: string
  mysql_mysqlx_worker_threads_metric_config:
    description: MysqlMysqlxWorkerThreadsMetricConfig provides config for the mysql.mysqlx_worker_threads metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_mysqlx_worker_threads_metric_attribute_key
      enabled:
        type: boolean
  mysql_opened_resources_metric_attribute_key:
    description: MysqlOpenedResourcesMetricAttributeKey specifies the key of an attribute for the mysql.opened_resources metric.
    type: string
  mysql_opened_resources_metric_config:
    description: MysqlOpenedResourcesMetricConfig provides config for the mysql.opened_resources metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_opened_resources_metric_attribute_key
      enabled:
        type: boolean
  mysql_operations_metric_attribute_key:
    description: MysqlOperationsMetricAttributeKey specifies the key of an attribute for the mysql.operations metric.
    type: string
  mysql_operations_metric_config:
    description: MysqlOperationsMetricConfig provides config for the mysql.operations metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_operations_metric_attribute_key
      enabled:
        type: boolean
  mysql_page_operations_metric_attribute_key:
    description: MysqlPageOperationsMetricAttributeKey specifies the key of an attribute for the mysql.page_operations metric.
    type: string
  mysql_page_operations_metric_config:
    description: MysqlPageOperationsMetricConfig provides config for the mysql.page_operations metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_page_operations_metric_attribute_key
      enabled:
        type: boolean
  mysql_page_size_metric_config:
    description: MysqlPageSizeMetricConfig provides config for the mysql.page_size metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_prepared_statements_metric_attribute_key:
    description: MysqlPreparedStatementsMetricAttributeKey specifies the key of an attribute for the mysql.prepared_statements metric.
    type: string
  mysql_prepared_statements_metric_config:
    description: MysqlPreparedStatementsMetricConfig provides config for the mysql.prepared_statements metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_prepared_statements_metric_attribute_key
      enabled:
        type: boolean
  mysql_query_client_count_metric_config:
    description: MysqlQueryClientCountMetricConfig provides config for the mysql.query.client.count metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_query_count_metric_config:
    description: MysqlQueryCountMetricConfig provides config for the mysql.query.count metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_query_slow_count_metric_config:
    description: MysqlQuerySlowCountMetricConfig provides config for the mysql.query.slow.count metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_replica_sql_delay_metric_config:
    description: MysqlReplicaSQLDelayMetricConfig provides config for the mysql.replica.sql_delay metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_replica_temp_table_open_metric_config:
    description: MysqlReplicaTempTableOpenMetricConfig provides config for the mysql.replica.temp_table.open metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_replica_thread_running_metric_attribute_key:
    description: MysqlReplicaThreadRunningMetricAttributeKey specifies the key of an attribute for the mysql.replica.thread.running metric.
    type: string
  mysql_replica_thread_running_metric_config:
    description: MysqlReplicaThreadRunningMetricConfig provides config for the mysql.replica.thread.running metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_replica_thread_running_metric_attribute_key
      enabled:
        type: boolean
  mysql_replica_time_behind_source_metric_config:
    description: MysqlReplicaTimeBehindSourceMetricConfig provides config for the mysql.replica.time_behind_source metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_row_locks_metric_attribute_key:
    description: MysqlRowLocksMetricAttributeKey specifies the key of an attribute for the mysql.row_locks metric.
    type: string
  mysql_row_locks_metric_config:
    description: MysqlRowLocksMetricConfig provides config for the mysql.row_locks metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_row_locks_metric_attribute_key
      enabled:
        type: boolean
  mysql_row_operations_metric_attribute_key:
    description: MysqlRowOperationsMetricAttributeKey specifies the key of an attribute for the mysql.row_operations metric.
    type: string
  mysql_row_operations_metric_config:
    description: MysqlRowOperationsMetricConfig provides config for the mysql.row_operations metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_row_operations_metric_attribute_key
      enabled:
        type: boolean
  mysql_sorts_metric_attribute_key:
    description: MysqlSortsMetricAttributeKey specifies the key of an attribute for the mysql.sorts metric.
    type: string
  mysql_sorts_metric_config:
    description: MysqlSortsMetricConfig provides config for the mysql.sorts metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_sorts_metric_attribute_key
      enabled:
        type: boolean
  mysql_statement_event_count_metric_attribute_key:
    description: MysqlStatementEventCountMetricAttributeKey specifies the key of an attribute for the mysql.statement_event.count metric.
    type: string
  mysql_statement_event_count_metric_config:
    description: MysqlStatementEventCountMetricConfig provides config for the mysql.statement_event.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_statement_event_count_metric_attribute_key
      enabled:
        type: boolean
  mysql_statement_event_wait_time_metric_attribute_key:
    description: MysqlStatementEventWaitTimeMetricAttributeKey specifies the key of an attribute for the mysql.statement_event.wait.time metric.
    type: string
  mysql_statement_event_wait_time_metric_config:
    description: MysqlStatementEventWaitTimeMetricConfig provides config for the mysql.statement_event.wait.time metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_statement_event_wait_time_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_average_row_length_metric_attribute_key:
    description: MysqlTableAverageRowLengthMetricAttributeKey specifies the key of an attribute for the mysql.table.average_row_length metric.
    type: string
  mysql_table_average_row_length_metric_config:
    description: MysqlTableAverageRowLengthMetricConfig provides config for the mysql.table.average_row_length metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_average_row_length_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_io_wait_count_metric_attribute_key:
    description: MysqlTableIoWaitCountMetricAttributeKey specifies the key of an attribute for the mysql.table.io.wait.count metric.
    type: string
  mysql_table_io_wait_count_metric_config:
    description: MysqlTableIoWaitCountMetricConfig provides config for the mysql.table.io.wait.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_io_wait_count_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_io_wait_time_metric_attribute_key:
    description: MysqlTableIoWaitTimeMetricAttributeKey specifies the key of an attribute for the mysql.table.io.wait.time metric.
    type: string
  mysql_table_io_wait_time_metric_config:
    description: MysqlTableIoWaitTimeMetricConfig provides config for the mysql.table.io.wait.time metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_io_wait_time_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_lock_wait_read_count_metric_attribute_key:
    description: MysqlTableLockWaitReadCountMetricAttributeKey specifies the key of an attribute for the mysql.table.lock_wait.read.count metric.
    type: string
  mysql_table_lock_wait_read_count_metric_config:
    description: MysqlTableLockWaitReadCountMetricConfig provides config for the mysql.table.lock_wait.read.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_lock_wait_read_count_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_lock_wait_read_time_metric_attribute_key:
    description: MysqlTableLockWaitReadTimeMetricAttributeKey specifies the key of an attribute for the mysql.table.lock_wait.read.time metric.
    type: string
  mysql_table_lock_wait_read_time_metric_config:
    description: MysqlTableLockWaitReadTimeMetricConfig provides config for the mysql.table.lock_wait.read.time metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_lock_wait_read_time_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_lock_wait_write_count_metric_attribute_key:
    description: MysqlTableLockWaitWriteCountMetricAttributeKey specifies the key of an attribute for the mysql.table.lock_wait.write.count metric.
    type: string
  mysql_table_lock_wait_write_count_metric_config:
    description: MysqlTableLockWaitWriteCountMetricConfig provides config for the mysql.table.lock_wait.write.count metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_lock_wait_write_count_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_lock_wait_write_time_metric_attribute_key:
    description: MysqlTableLockWaitWriteTimeMetricAttributeKey specifies the key of an attribute for the mysql.table.lock_wait.write.time metric.
    type: string
  mysql_table_lock_wait_write_time_metric_config:
    description: MysqlTableLockWaitWriteTimeMetricConfig provides config for the mysql.table.lock_wait.write.time metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_lock_wait_write_time_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_open_cache_metric_attribute_key:
    description: MysqlTableOpenCacheMetricAttributeKey specifies the key of an attribute for the mysql.table_open_cache metric.
    type: string
  mysql_table_open_cache_metric_config:
    description: MysqlTableOpenCacheMetricConfig provides config for the mysql.table_open_cache metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_open_cache_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_open_metric_config:
    description: MysqlTableOpenMetricConfig provides config for the mysql.table.open metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_table_rows_metric_attribute_key:
    description: MysqlTableRowsMetricAttributeKey specifies the key of an attribute for the mysql.table.rows metric.
    type: string
  mysql_table_rows_metric_config:
    description: MysqlTableRowsMetricConfig provides config for the mysql.table.rows metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_rows_metric_attribute_key
      enabled:
        type: boolean
  mysql_table_size_metric_attribute_key:
    description: MysqlTableSizeMetricAttributeKey specifies the key of an attribute for the mysql.table.size metric.
    type: string
  mysql_table_size_metric_config:
    description: MysqlTableSizeMetricConfig provides config for the mysql.table.size metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_table_size_metric_attribute_key
      enabled:
        type: boolean
  mysql_thread_slow_launch_metric_config:
    description: MysqlThreadSlowLaunchMetricConfig provides config for the mysql.thread.slow_launch metric.
    type: object
    properties:
      enabled:
        type: boolean
  mysql_threads_metric_attribute_key:
    description: MysqlThreadsMetricAttributeKey specifies the key of an attribute for the mysql.threads metric.
    type: string
  mysql_threads_metric_config:
    description: MysqlThreadsMetricConfig provides config for the mysql.threads metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_threads_metric_attribute_key
      enabled:
        type: boolean
  mysql_tmp_resources_metric_attribute_key:
    description: MysqlTmpResourcesMetricAttributeKey specifies the key of an attribute for the mysql.tmp_resources metric.
    type: string
  mysql_tmp_resources_metric_config:
    description: MysqlTmpResourcesMetricConfig provides config for the mysql.tmp_resources metric.
    type: object
    properties:
      aggregation_strategy:
        type: string
      attributes:
        type: array
        items:
          $ref: mysql_tmp_resources_metric_attribute_key
      enabled:
        type: boolean
  mysql_uptime_metric_config:
    description: MysqlUptimeMetricConfig provides config for the mysql.uptime metric.
    type: object
    properties:
      enabled:
        type: boolean
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for mysql resource attributes.
    type: object
    properties:
      db.system.name:
        $ref: db_system_name_resource_attribute_config
      db.system.version:
        $ref: db_system_version_resource_attribute_config
      mysql.instance.endpoint:
        $ref: mysql_instance_endpoint_resource_attribute_config
      service.instance.id:
        $ref: service_instance_id_resource_attribute_config
      service.name:
        $ref: service_name_resource_attribute_config
      service.namespace:
        $ref: service_namespace_resource_attribute_config
  service_instance_id_resource_attribute_config:
    description: ServiceInstanceIDResourceAttributeConfig provides config for the service.instance.id resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  service_name_resource_attribute_config:
    description: ServiceNameResourceAttributeConfig provides config for the service.name resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
  service_namespace_resource_attribute_config:
    description: ServiceNamespaceResourceAttributeConfig provides config for the service.namespace resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
      events_exclude:
        description: 'Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      events_include:
        description: 'Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_exclude:
        description: 'Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      metrics_include:
        description: 'Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted.'
        type: array
        items:
          $ref: go.opentelemetry.io/collector/filter.config
      override_value:
        description: OverrideValue allows users to override the value of this resource attribute.
        x-pointer: true
        type: string
