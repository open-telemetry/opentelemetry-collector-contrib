# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for mysql metrics.
    type: object
    properties:
      mysql.buffer_pool.data_pages:
        description: "MysqlBufferPoolDataPagesConfig provides config for the mysql.buffer_pool.data_pages metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "status"
            default:
              - "status"
      mysql.buffer_pool.limit:
        description: "MysqlBufferPoolLimitConfig provides config for the mysql.buffer_pool.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mysql.buffer_pool.operations:
        description: "MysqlBufferPoolOperationsConfig provides config for the mysql.buffer_pool.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
            default:
              - "operation"
      mysql.buffer_pool.page_flushes:
        description: "MysqlBufferPoolPageFlushesConfig provides config for the mysql.buffer_pool.page_flushes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mysql.buffer_pool.pages:
        description: "MysqlBufferPoolPagesConfig provides config for the mysql.buffer_pool.pages metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.buffer_pool.usage:
        description: "MysqlBufferPoolUsageConfig provides config for the mysql.buffer_pool.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "status"
            default:
              - "status"
      mysql.client.network.io:
        description: "MysqlClientNetworkIoConfig provides config for the mysql.client.network.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.commands:
        description: "MysqlCommandsConfig provides config for the mysql.commands metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "command"
            default:
              - "command"
      mysql.connection.count:
        description: "MysqlConnectionCountConfig provides config for the mysql.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.connection.errors:
        description: "MysqlConnectionErrorsConfig provides config for the mysql.connection.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "error"
            default:
              - "error"
      mysql.double_writes:
        description: "MysqlDoubleWritesConfig provides config for the mysql.double_writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.handlers:
        description: "MysqlHandlersConfig provides config for the mysql.handlers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.index.io.wait.count:
        description: "MysqlIndexIoWaitCountConfig provides config for the mysql.index.io.wait.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
                - "table"
                - "schema"
                - "index"
            default:
              - "operation"
              - "table"
              - "schema"
              - "index"
      mysql.index.io.wait.time:
        description: "MysqlIndexIoWaitTimeConfig provides config for the mysql.index.io.wait.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
                - "table"
                - "schema"
                - "index"
            default:
              - "operation"
              - "table"
              - "schema"
              - "index"
      mysql.joins:
        description: "MysqlJoinsConfig provides config for the mysql.joins metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.locks:
        description: "MysqlLocksConfig provides config for the mysql.locks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.log_operations:
        description: "MysqlLogOperationsConfig provides config for the mysql.log_operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
            default:
              - "operation"
      mysql.max_used_connections:
        description: "MysqlMaxUsedConnectionsConfig provides config for the mysql.max_used_connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.mysqlx_connections:
        description: "MysqlMysqlxConnectionsConfig provides config for the mysql.mysqlx_connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "status"
            default:
              - "status"
      mysql.mysqlx_worker_threads:
        description: "MysqlMysqlxWorkerThreadsConfig provides config for the mysql.mysqlx_worker_threads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.opened_resources:
        description: "MysqlOpenedResourcesConfig provides config for the mysql.opened_resources metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.operations:
        description: "MysqlOperationsConfig provides config for the mysql.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
            default:
              - "operation"
      mysql.page_operations:
        description: "MysqlPageOperationsConfig provides config for the mysql.page_operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
            default:
              - "operation"
      mysql.page_size:
        description: "MysqlPageSizeConfig provides config for the mysql.page_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.prepared_statements:
        description: "MysqlPreparedStatementsConfig provides config for the mysql.prepared_statements metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "command"
            default:
              - "command"
      mysql.query.client.count:
        description: "MysqlQueryClientCountConfig provides config for the mysql.query.client.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.query.count:
        description: "MysqlQueryCountConfig provides config for the mysql.query.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.query.slow.count:
        description: "MysqlQuerySlowCountConfig provides config for the mysql.query.slow.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.replica.sql_delay:
        description: "MysqlReplicaSQLDelayConfig provides config for the mysql.replica.sql_delay metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.replica.time_behind_source:
        description: "MysqlReplicaTimeBehindSourceConfig provides config for the mysql.replica.time_behind_source metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mysql.row_locks:
        description: "MysqlRowLocksConfig provides config for the mysql.row_locks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.row_operations:
        description: "MysqlRowOperationsConfig provides config for the mysql.row_operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
            default:
              - "operation"
      mysql.sorts:
        description: "MysqlSortsConfig provides config for the mysql.sorts metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.statement_event.count:
        description: "MysqlStatementEventCountConfig provides config for the mysql.statement_event.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema"
                - "digest"
                - "digest_text"
                - "kind"
            default:
              - "schema"
              - "digest"
              - "digest_text"
              - "kind"
      mysql.statement_event.wait.time:
        description: "MysqlStatementEventWaitTimeConfig provides config for the mysql.statement_event.wait.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema"
                - "digest"
                - "digest_text"
            default:
              - "schema"
              - "digest"
              - "digest_text"
      mysql.table.average_row_length:
        description: "MysqlTableAverageRowLengthConfig provides config for the mysql.table.average_row_length metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "table"
                - "schema"
            default:
              - "table"
              - "schema"
      mysql.table.io.wait.count:
        description: "MysqlTableIoWaitCountConfig provides config for the mysql.table.io.wait.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
                - "table"
                - "schema"
            default:
              - "operation"
              - "table"
              - "schema"
      mysql.table.io.wait.time:
        description: "MysqlTableIoWaitTimeConfig provides config for the mysql.table.io.wait.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
                - "table"
                - "schema"
            default:
              - "operation"
              - "table"
              - "schema"
      mysql.table.lock_wait.read.count:
        description: "MysqlTableLockWaitReadCountConfig provides config for the mysql.table.lock_wait.read.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema"
                - "table"
                - "kind"
            default:
              - "schema"
              - "table"
              - "kind"
      mysql.table.lock_wait.read.time:
        description: "MysqlTableLockWaitReadTimeConfig provides config for the mysql.table.lock_wait.read.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema"
                - "table"
                - "kind"
            default:
              - "schema"
              - "table"
              - "kind"
      mysql.table.lock_wait.write.count:
        description: "MysqlTableLockWaitWriteCountConfig provides config for the mysql.table.lock_wait.write.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema"
                - "table"
                - "kind"
            default:
              - "schema"
              - "table"
              - "kind"
      mysql.table.lock_wait.write.time:
        description: "MysqlTableLockWaitWriteTimeConfig provides config for the mysql.table.lock_wait.write.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema"
                - "table"
                - "kind"
            default:
              - "schema"
              - "table"
              - "kind"
      mysql.table.rows:
        description: "MysqlTableRowsConfig provides config for the mysql.table.rows metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "table"
                - "schema"
            default:
              - "table"
              - "schema"
      mysql.table.size:
        description: "MysqlTableSizeConfig provides config for the mysql.table.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "table"
                - "schema"
                - "kind"
            default:
              - "table"
              - "schema"
              - "kind"
      mysql.table_open_cache:
        description: "MysqlTableOpenCacheConfig provides config for the mysql.table_open_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "status"
            default:
              - "status"
      mysql.threads:
        description: "MysqlThreadsConfig provides config for the mysql.threads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "kind"
            default:
              - "kind"
      mysql.tmp_resources:
        description: "MysqlTmpResourcesConfig provides config for the mysql.tmp_resources metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "resource"
            default:
              - "resource"
      mysql.uptime:
        description: "MysqlUptimeConfig provides config for the mysql.uptime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  events_config:
    description: EventsConfig provides config for mysql events.
    type: object
    properties:
      db.server.query_sample:
        description: EventConfig provides common config for a  db.server.query_sample event.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      db.server.top_query:
        description: EventConfig provides common config for a  db.server.top_query event.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for mysql resource attributes.
    type: object
    properties:
      mysql.instance.endpoint:
        description: ResourceAttributeConfig provides common config for a mysql.instance.endpoint resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for mysql metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for mysql logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
