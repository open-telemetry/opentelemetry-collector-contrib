type: udplog

status:
  class: receiver
  stability:
    alpha: [logs]
  distributions: [contrib, observiq, sumo, splunk]
  codeowners:
    active: [djaglowski]

tests:
  config:
    listen_address: "localhost:0"
