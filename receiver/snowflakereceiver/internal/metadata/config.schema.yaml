# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for snowflake metrics.
    type: object
    properties:
      snowflake.billing.cloud_service.total:
        description: "SnowflakeBillingCloudServiceTotalConfig provides config for the snowflake.billing.cloud_service.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "service_type"
            default:
              - "service_type"
      snowflake.billing.total_credit.total:
        description: "SnowflakeBillingTotalCreditTotalConfig provides config for the snowflake.billing.total_credit.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "service_type"
            default:
              - "service_type"
      snowflake.billing.virtual_warehouse.total:
        description: "SnowflakeBillingVirtualWarehouseTotalConfig provides config for the snowflake.billing.virtual_warehouse.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "service_type"
            default:
              - "service_type"
      snowflake.billing.warehouse.cloud_service.total:
        description: "SnowflakeBillingWarehouseCloudServiceTotalConfig provides config for the snowflake.billing.warehouse.cloud_service.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.billing.warehouse.total_credit.total:
        description: "SnowflakeBillingWarehouseTotalCreditTotalConfig provides config for the snowflake.billing.warehouse.total_credit.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.billing.warehouse.virtual_warehouse.total:
        description: "SnowflakeBillingWarehouseVirtualWarehouseTotalConfig provides config for the snowflake.billing.warehouse.virtual_warehouse.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.database.bytes_scanned.avg:
        description: "SnowflakeDatabaseBytesScannedAvgConfig provides config for the snowflake.database.bytes_scanned.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.database.query.count:
        description: "SnowflakeDatabaseQueryCountConfig provides config for the snowflake.database.query.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.logins.total:
        description: "SnowflakeLoginsTotalConfig provides config for the snowflake.logins.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "error_message"
                - "reported_client_type"
                - "is_success"
            default:
              - "error_message"
              - "reported_client_type"
              - "is_success"
      snowflake.pipe.credits_used.total:
        description: "SnowflakePipeCreditsUsedTotalConfig provides config for the snowflake.pipe.credits_used.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "pipe_name"
            default:
              - "pipe_name"
      snowflake.query.blocked:
        description: "SnowflakeQueryBlockedConfig provides config for the snowflake.query.blocked metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.query.bytes_deleted.avg:
        description: "SnowflakeQueryBytesDeletedAvgConfig provides config for the snowflake.query.bytes_deleted.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.bytes_spilled.local.avg:
        description: "SnowflakeQueryBytesSpilledLocalAvgConfig provides config for the snowflake.query.bytes_spilled.local.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.bytes_spilled.remote.avg:
        description: "SnowflakeQueryBytesSpilledRemoteAvgConfig provides config for the snowflake.query.bytes_spilled.remote.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.bytes_written.avg:
        description: "SnowflakeQueryBytesWrittenAvgConfig provides config for the snowflake.query.bytes_written.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.compilation_time.avg:
        description: "SnowflakeQueryCompilationTimeAvgConfig provides config for the snowflake.query.compilation_time.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.data_scanned_cache.avg:
        description: "SnowflakeQueryDataScannedCacheAvgConfig provides config for the snowflake.query.data_scanned_cache.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.executed:
        description: "SnowflakeQueryExecutedConfig provides config for the snowflake.query.executed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.query.execution_time.avg:
        description: "SnowflakeQueryExecutionTimeAvgConfig provides config for the snowflake.query.execution_time.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.partitions_scanned.avg:
        description: "SnowflakeQueryPartitionsScannedAvgConfig provides config for the snowflake.query.partitions_scanned.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.query.queued_overload:
        description: "SnowflakeQueryQueuedOverloadConfig provides config for the snowflake.query.queued_overload metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.query.queued_provision:
        description: "SnowflakeQueryQueuedProvisionConfig provides config for the snowflake.query.queued_provision metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "warehouse_name"
            default:
              - "warehouse_name"
      snowflake.queued_overload_time.avg:
        description: "SnowflakeQueuedOverloadTimeAvgConfig provides config for the snowflake.queued_overload_time.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.queued_provisioning_time.avg:
        description: "SnowflakeQueuedProvisioningTimeAvgConfig provides config for the snowflake.queued_provisioning_time.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.queued_repair_time.avg:
        description: "SnowflakeQueuedRepairTimeAvgConfig provides config for the snowflake.queued_repair_time.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.rows_deleted.avg:
        description: "SnowflakeRowsDeletedAvgConfig provides config for the snowflake.rows_deleted.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.rows_inserted.avg:
        description: "SnowflakeRowsInsertedAvgConfig provides config for the snowflake.rows_inserted.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.rows_produced.avg:
        description: "SnowflakeRowsProducedAvgConfig provides config for the snowflake.rows_produced.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.rows_unloaded.avg:
        description: "SnowflakeRowsUnloadedAvgConfig provides config for the snowflake.rows_unloaded.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.rows_updated.avg:
        description: "SnowflakeRowsUpdatedAvgConfig provides config for the snowflake.rows_updated.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
      snowflake.session_id.count:
        description: "SnowflakeSessionIDCountConfig provides config for the snowflake.session_id.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "user_name"
            default:
              - "user_name"
      snowflake.storage.failsafe_bytes.total:
        description: "SnowflakeStorageFailsafeBytesTotalConfig provides config for the snowflake.storage.failsafe_bytes.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      snowflake.storage.stage_bytes.total:
        description: "SnowflakeStorageStageBytesTotalConfig provides config for the snowflake.storage.stage_bytes.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      snowflake.storage.storage_bytes.total:
        description: "SnowflakeStorageStorageBytesTotalConfig provides config for the snowflake.storage.storage_bytes.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      snowflake.total_elapsed_time.avg:
        description: "SnowflakeTotalElapsedTimeAvgConfig provides config for the snowflake.total_elapsed_time.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "schema_name"
                - "execution_status"
                - "error_message"
                - "query_type"
                - "warehouse_name"
                - "database_name"
                - "warehouse_size"
            default:
              - "schema_name"
              - "execution_status"
              - "error_message"
              - "query_type"
              - "warehouse_name"
              - "database_name"
              - "warehouse_size"
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for snowflake resource attributes.
    type: object
    properties:
      snowflake.account.name:
        description: ResourceAttributeConfig provides common config for a snowflake.account.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for snowflake metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
