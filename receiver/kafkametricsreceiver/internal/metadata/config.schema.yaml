# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for kafkametrics metrics.
    type: object
    properties:
      kafka.broker.log_retention_period:
        description: "KafkaBrokerLogRetentionPeriodConfig provides config for the kafka.broker.log_retention_period metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "broker"
            default:
              - "broker"
      kafka.brokers:
        description: "KafkaBrokersConfig provides config for the kafka.brokers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      kafka.consumer_group.lag:
        description: "KafkaConsumerGroupLagConfig provides config for the kafka.consumer_group.lag metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "group"
                - "topic"
                - "partition"
            default:
              - "group"
              - "topic"
              - "partition"
      kafka.consumer_group.lag_sum:
        description: "KafkaConsumerGroupLagSumConfig provides config for the kafka.consumer_group.lag_sum metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "group"
                - "topic"
            default:
              - "group"
              - "topic"
      kafka.consumer_group.members:
        description: "KafkaConsumerGroupMembersConfig provides config for the kafka.consumer_group.members metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "group"
            default:
              - "group"
      kafka.consumer_group.offset:
        description: "KafkaConsumerGroupOffsetConfig provides config for the kafka.consumer_group.offset metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "group"
                - "topic"
                - "partition"
            default:
              - "group"
              - "topic"
              - "partition"
      kafka.consumer_group.offset_sum:
        description: "KafkaConsumerGroupOffsetSumConfig provides config for the kafka.consumer_group.offset_sum metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "group"
                - "topic"
            default:
              - "group"
              - "topic"
      kafka.partition.current_offset:
        description: "KafkaPartitionCurrentOffsetConfig provides config for the kafka.partition.current_offset metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
                - "partition"
            default:
              - "topic"
              - "partition"
      kafka.partition.oldest_offset:
        description: "KafkaPartitionOldestOffsetConfig provides config for the kafka.partition.oldest_offset metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
                - "partition"
            default:
              - "topic"
              - "partition"
      kafka.partition.replicas:
        description: "KafkaPartitionReplicasConfig provides config for the kafka.partition.replicas metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
                - "partition"
            default:
              - "topic"
              - "partition"
      kafka.partition.replicas_in_sync:
        description: "KafkaPartitionReplicasInSyncConfig provides config for the kafka.partition.replicas_in_sync metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
                - "partition"
            default:
              - "topic"
              - "partition"
      kafka.topic.log_retention_period:
        description: "KafkaTopicLogRetentionPeriodConfig provides config for the kafka.topic.log_retention_period metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
            default:
              - "topic"
      kafka.topic.log_retention_size:
        description: "KafkaTopicLogRetentionSizeConfig provides config for the kafka.topic.log_retention_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
            default:
              - "topic"
      kafka.topic.min_insync_replicas:
        description: "KafkaTopicMinInsyncReplicasConfig provides config for the kafka.topic.min_insync_replicas metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
            default:
              - "topic"
      kafka.topic.partitions:
        description: "KafkaTopicPartitionsConfig provides config for the kafka.topic.partitions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
            default:
              - "topic"
      kafka.topic.replication_factor:
        description: "KafkaTopicReplicationFactorConfig provides config for the kafka.topic.replication_factor metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "topic"
            default:
              - "topic"
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for kafkametrics resource attributes.
    type: object
    properties:
      kafka.cluster.alias:
        description: ResourceAttributeConfig provides common config for a kafka.cluster.alias resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for kafkametrics metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
