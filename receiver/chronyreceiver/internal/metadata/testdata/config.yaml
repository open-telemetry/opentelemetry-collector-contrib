default:
all_set:
  metrics:
    ntp.frequency.offset:
      enabled: true
    ntp.skew:
      enabled: true
    ntp.stratum:
      enabled: true
    ntp.time.correction:
      enabled: true
    ntp.time.last_offset:
      enabled: true
    ntp.time.rms_offset:
      enabled: true
    ntp.time.root_delay:
      enabled: true
none_set:
  metrics:
    ntp.frequency.offset:
      enabled: false
    ntp.skew:
      enabled: false
    ntp.stratum:
      enabled: false
    ntp.time.correction:
      enabled: false
    ntp.time.last_offset:
      enabled: false
    ntp.time.rms_offset:
      enabled: false
    ntp.time.root_delay:
      enabled: false
