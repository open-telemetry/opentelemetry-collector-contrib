# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for sshcheck metrics.
    type: object
    properties:
      sshcheck.duration:
        description: "SshcheckDurationConfig provides config for the sshcheck.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      sshcheck.error:
        description: "SshcheckErrorConfig provides config for the sshcheck.error metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      sshcheck.sftp_duration:
        description: "SshcheckSftpDurationConfig provides config for the sshcheck.sftp_duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      sshcheck.sftp_error:
        description: "SshcheckSftpErrorConfig provides config for the sshcheck.sftp_error metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      sshcheck.sftp_status:
        description: "SshcheckSftpStatusConfig provides config for the sshcheck.sftp_status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      sshcheck.status:
        description: "SshcheckStatusConfig provides config for the sshcheck.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for sshcheck resource attributes.
    type: object
    properties:
      ssh.endpoint:
        description: ResourceAttributeConfig provides common config for a ssh.endpoint resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for sshcheck metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
