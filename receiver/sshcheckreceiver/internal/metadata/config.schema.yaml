# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for ssh_check metrics.
    type: object
    properties:
      sshcheck.duration:
        description: "SshcheckDurationMetricConfig provides config for the sshcheck.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      sshcheck.error:
        description: "SshcheckErrorMetricConfig provides config for the sshcheck.error metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "error.message"
            default:
              - "error.message"
      sshcheck.sftp_duration:
        description: "SshcheckSftpDurationMetricConfig provides config for the sshcheck.sftp_duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      sshcheck.sftp_error:
        description: "SshcheckSftpErrorMetricConfig provides config for the sshcheck.sftp_error metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "error.message"
            default:
              - "error.message"
      sshcheck.sftp_status:
        description: "SshcheckSftpStatusMetricConfig provides config for the sshcheck.sftp_status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      sshcheck.status:
        description: "SshcheckStatusMetricConfig provides config for the sshcheck.status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for ssh_check resource attributes.
    type: object
    properties:
      ssh.endpoint:
        description: ResourceAttributeConfig provides common config for a ssh.endpoint resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for ssh_check metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
