type: carbon

status:
  class: receiver
  stability:
    beta: [metrics]
  distributions: [contrib]
  codeowners:
    active: [aboguszewski-sumo]

# TODO: Update the exporter to pass the tests
tests:
  skip_lifecycle: true
