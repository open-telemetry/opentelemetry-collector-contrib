type: skywalking
scope_name: otelcol/skywalkingreceiver

status:
  class: receiver
  stability:
    development: [metrics]
    beta: [traces]
  distributions: [contrib, sumo]
  codeowners:
    active: [JaredTan95]

tests:
  config:
    protocols:
      grpc:
        endpoint: localhost:1234