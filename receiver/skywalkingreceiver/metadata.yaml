type: skywalking

status:
  class: receiver
  stability:
    development: [metrics]
    beta: [traces]
  distributions: [contrib]
  codeowners:
    active: [JaredTan95]

tests:
  config:
    protocols:
      grpc:
        endpoint: localhost:1234