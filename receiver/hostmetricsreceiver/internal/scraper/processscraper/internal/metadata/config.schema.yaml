# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for process metrics.
    type: object
    properties:
      process.context_switches:
        description: "ProcessContextSwitchesMetricConfig provides config for the process.context_switches metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.cpu.time:
        description: "ProcessCPUTimeMetricConfig provides config for the process.cpu.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      process.cpu.utilization:
        description: "ProcessCPUUtilizationMetricConfig provides config for the process.cpu.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.disk.io:
        description: "ProcessDiskIoMetricConfig provides config for the process.disk.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      process.disk.operations:
        description: "ProcessDiskOperationsMetricConfig provides config for the process.disk.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.handles:
        description: "ProcessHandlesMetricConfig provides config for the process.handles metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.memory.usage:
        description: "ProcessMemoryUsageMetricConfig provides config for the process.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      process.memory.utilization:
        description: "ProcessMemoryUtilizationMetricConfig provides config for the process.memory.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.memory.virtual:
        description: "ProcessMemoryVirtualMetricConfig provides config for the process.memory.virtual metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      process.open_file_descriptors:
        description: "ProcessOpenFileDescriptorsMetricConfig provides config for the process.open_file_descriptors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.paging.faults:
        description: "ProcessPagingFaultsMetricConfig provides config for the process.paging.faults metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.signals_pending:
        description: "ProcessSignalsPendingMetricConfig provides config for the process.signals_pending metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.threads:
        description: "ProcessThreadsMetricConfig provides config for the process.threads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      process.uptime:
        description: "ProcessUptimeMetricConfig provides config for the process.uptime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for process resource attributes.
    type: object
    properties:
      process.cgroup:
        description: ResourceAttributeConfig provides common config for a process.cgroup resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.command:
        description: ResourceAttributeConfig provides common config for a process.command resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.command_line:
        description: ResourceAttributeConfig provides common config for a process.command_line resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.executable.name:
        description: ResourceAttributeConfig provides common config for a process.executable.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.executable.path:
        description: ResourceAttributeConfig provides common config for a process.executable.path resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.owner:
        description: ResourceAttributeConfig provides common config for a process.owner resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.parent_pid:
        description: ResourceAttributeConfig provides common config for a process.parent_pid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      process.pid:
        description: ResourceAttributeConfig provides common config for a process.pid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for process metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
