# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for hardware metrics.
    type: object
    properties:
      hw.temperature:
        description: "HwTemperatureMetricConfig provides config for the hw.temperature metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      hw.temperature.limit:
        description: "HwTemperatureLimitMetricConfig provides config for the hw.temperature.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for hardware metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
