# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for processes metrics.
    type: object
    properties:
      system.processes.count:
        description: "SystemProcessesCountConfig provides config for the system.processes.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "status"
            default:
              - "status"
      system.processes.created:
        description: "SystemProcessesCreatedConfig provides config for the system.processes.created metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for processes metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
