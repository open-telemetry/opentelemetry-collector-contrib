# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for load metrics.
    type: object
    properties:
      system.cpu.load_average.15m:
        description: "SystemCPULoadAverage15mMetricConfig provides config for the system.cpu.load_average.15m metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      system.cpu.load_average.1m:
        description: "SystemCPULoadAverage1mMetricConfig provides config for the system.cpu.load_average.1m metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      system.cpu.load_average.5m:
        description: "SystemCPULoadAverage5mMetricConfig provides config for the system.cpu.load_average.5m metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for load metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
