# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for memory metrics.
    type: object
    properties:
      system.linux.memory.available:
        description: "SystemLinuxMemoryAvailableConfig provides config for the system.linux.memory.available metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.linux.memory.dirty:
        description: "SystemLinuxMemoryDirtyConfig provides config for the system.linux.memory.dirty metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.limit:
        description: "SystemMemoryLimitConfig provides config for the system.memory.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.linux.hugepages.limit:
        description: "SystemMemoryLinuxHugepagesLimitConfig provides config for the system.memory.linux.hugepages.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.linux.hugepages.page_size:
        description: "SystemMemoryLinuxHugepagesPageSizeConfig provides config for the system.memory.linux.hugepages.page_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.linux.hugepages.reserved:
        description: "SystemMemoryLinuxHugepagesReservedConfig provides config for the system.memory.linux.hugepages.reserved metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.linux.hugepages.surplus:
        description: "SystemMemoryLinuxHugepagesSurplusConfig provides config for the system.memory.linux.hugepages.surplus metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.linux.hugepages.usage:
        description: "SystemMemoryLinuxHugepagesUsageConfig provides config for the system.memory.linux.hugepages.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "system.memory.linux.hugepages.state"
            default:
              - "system.memory.linux.hugepages.state"
      system.memory.linux.hugepages.utilization:
        description: "SystemMemoryLinuxHugepagesUtilizationConfig provides config for the system.memory.linux.hugepages.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "system.memory.linux.hugepages.state"
            default:
              - "system.memory.linux.hugepages.state"
      system.memory.linux.shared:
        description: "SystemMemoryLinuxSharedConfig provides config for the system.memory.linux.shared metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.page_size:
        description: "SystemMemoryPageSizeConfig provides config for the system.memory.page_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.memory.usage:
        description: "SystemMemoryUsageConfig provides config for the system.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "state"
            default:
              - "state"
      system.memory.utilization:
        description: "SystemMemoryUtilizationConfig provides config for the system.memory.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "state"
            default:
              - "state"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for memory metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
