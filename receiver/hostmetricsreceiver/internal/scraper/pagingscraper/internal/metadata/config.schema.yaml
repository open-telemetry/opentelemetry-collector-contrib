# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for paging metrics.
    type: object
    properties:
      system.paging.faults:
        description: "SystemPagingFaultsMetricConfig provides config for the system.paging.faults metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
            default:
              - "type"
      system.paging.operations:
        description: "SystemPagingOperationsMetricConfig provides config for the system.paging.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "direction"
                - "type"
            default:
              - "direction"
              - "type"
      system.paging.usage:
        description: "SystemPagingUsageMetricConfig provides config for the system.paging.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "state"
            default:
              - "device"
              - "state"
      system.paging.utilization:
        description: "SystemPagingUtilizationMetricConfig provides config for the system.paging.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "state"
            default:
              - "device"
              - "state"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for paging metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
