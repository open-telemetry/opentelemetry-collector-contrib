# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for cpu metrics.
    type: object
    properties:
      system.cpu.frequency:
        description: "SystemCPUFrequencyConfig provides config for the system.cpu.frequency metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "cpu"
            default:
              - "cpu"
      system.cpu.logical.count:
        description: "SystemCPULogicalCountConfig provides config for the system.cpu.logical.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.cpu.physical.count:
        description: "SystemCPUPhysicalCountConfig provides config for the system.cpu.physical.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.cpu.time:
        description: "SystemCPUTimeConfig provides config for the system.cpu.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "cpu"
                - "state"
            default:
              - "cpu"
              - "state"
      system.cpu.utilization:
        description: "SystemCPUUtilizationConfig provides config for the system.cpu.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "cpu"
                - "state"
            default:
              - "cpu"
              - "state"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for cpu metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
