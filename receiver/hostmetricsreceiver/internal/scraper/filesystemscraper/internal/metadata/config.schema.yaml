# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for filesystem metrics.
    type: object
    properties:
      system.filesystem.inodes.usage:
        description: "SystemFilesystemInodesUsageMetricConfig provides config for the system.filesystem.inodes.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "mode"
                - "mountpoint"
                - "type"
                - "state"
            default:
              - "device"
              - "mode"
              - "mountpoint"
              - "type"
              - "state"
      system.filesystem.usage:
        description: "SystemFilesystemUsageMetricConfig provides config for the system.filesystem.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "mode"
                - "mountpoint"
                - "type"
                - "state"
            default:
              - "device"
              - "mode"
              - "mountpoint"
              - "type"
              - "state"
      system.filesystem.utilization:
        description: "SystemFilesystemUtilizationMetricConfig provides config for the system.filesystem.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "mode"
                - "mountpoint"
                - "type"
            default:
              - "device"
              - "mode"
              - "mountpoint"
              - "type"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for filesystem metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
