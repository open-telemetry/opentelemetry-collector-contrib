# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for nfs metrics.
    type: object
    properties:
      nfs.client.net.count:
        description: "NfsClientNetCountMetricConfig provides config for the nfs.client.net.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "network.transport"
            default:
              - "network.transport"
      nfs.client.net.tcp.connection.accepted:
        description: "NfsClientNetTCPConnectionAcceptedMetricConfig provides config for the nfs.client.net.tcp.connection.accepted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.client.operation.count:
        description: "NfsClientOperationCountMetricConfig provides config for the nfs.client.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "onc_rpc.version"
                - "nfs.operation.name"
            default:
              - "onc_rpc.version"
              - "nfs.operation.name"
      nfs.client.procedure.count:
        description: "NfsClientProcedureCountMetricConfig provides config for the nfs.client.procedure.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "onc_rpc.version"
                - "onc_rpc.procedure.name"
            default:
              - "onc_rpc.version"
              - "onc_rpc.procedure.name"
      nfs.client.rpc.authrefresh.count:
        description: "NfsClientRPCAuthrefreshCountMetricConfig provides config for the nfs.client.rpc.authrefresh.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.client.rpc.count:
        description: "NfsClientRPCCountMetricConfig provides config for the nfs.client.rpc.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.client.rpc.retransmit.count:
        description: "NfsClientRPCRetransmitCountMetricConfig provides config for the nfs.client.rpc.retransmit.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.server.fh.stale.count:
        description: "NfsServerFhStaleCountMetricConfig provides config for the nfs.server.fh.stale.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.server.io:
        description: "NfsServerIoMetricConfig provides config for the nfs.server.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "network.io.direction"
            default:
              - "network.io.direction"
      nfs.server.net.count:
        description: "NfsServerNetCountMetricConfig provides config for the nfs.server.net.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "network.transport"
            default:
              - "network.transport"
      nfs.server.net.tcp.connection.accepted:
        description: "NfsServerNetTCPConnectionAcceptedMetricConfig provides config for the nfs.server.net.tcp.connection.accepted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.server.operation.count:
        description: "NfsServerOperationCountMetricConfig provides config for the nfs.server.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "onc_rpc.version"
                - "nfs.operation.name"
            default:
              - "onc_rpc.version"
              - "nfs.operation.name"
      nfs.server.procedure.count:
        description: "NfsServerProcedureCountMetricConfig provides config for the nfs.server.procedure.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "onc_rpc.version"
                - "onc_rpc.procedure.name"
            default:
              - "onc_rpc.version"
              - "onc_rpc.procedure.name"
      nfs.server.repcache.requests:
        description: "NfsServerRepcacheRequestsMetricConfig provides config for the nfs.server.repcache.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "nfs.server.repcache.status"
            default:
              - "nfs.server.repcache.status"
      nfs.server.rpc.count:
        description: "NfsServerRPCCountMetricConfig provides config for the nfs.server.rpc.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "error.type"
            default:
              - "error.type"
      nfs.server.thread.count:
        description: "NfsServerThreadCountMetricConfig provides config for the nfs.server.thread.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for nfs metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
