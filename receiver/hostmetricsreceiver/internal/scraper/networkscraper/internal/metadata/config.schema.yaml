# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for network metrics.
    type: object
    properties:
      system.network.connections:
        description: "SystemNetworkConnectionsMetricConfig provides config for the system.network.connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "protocol"
                - "state"
            default:
              - "protocol"
              - "state"
      system.network.conntrack.count:
        description: "SystemNetworkConntrackCountMetricConfig provides config for the system.network.conntrack.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.network.conntrack.max:
        description: "SystemNetworkConntrackMaxMetricConfig provides config for the system.network.conntrack.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      system.network.dropped:
        description: "SystemNetworkDroppedMetricConfig provides config for the system.network.dropped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.network.errors:
        description: "SystemNetworkErrorsMetricConfig provides config for the system.network.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.network.io:
        description: "SystemNetworkIoMetricConfig provides config for the system.network.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.network.packets:
        description: "SystemNetworkPacketsMetricConfig provides config for the system.network.packets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for network metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
