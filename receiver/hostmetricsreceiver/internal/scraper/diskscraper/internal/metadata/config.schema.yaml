# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for disk metrics.
    type: object
    properties:
      system.disk.io:
        description: "SystemDiskIoMetricConfig provides config for the system.disk.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.disk.io_time:
        description: "SystemDiskIoTimeMetricConfig provides config for the system.disk.io_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
            default:
              - "device"
      system.disk.merged:
        description: "SystemDiskMergedMetricConfig provides config for the system.disk.merged metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.disk.operation_time:
        description: "SystemDiskOperationTimeMetricConfig provides config for the system.disk.operation_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.disk.operations:
        description: "SystemDiskOperationsMetricConfig provides config for the system.disk.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
                - "direction"
            default:
              - "device"
              - "direction"
      system.disk.pending_operations:
        description: "SystemDiskPendingOperationsMetricConfig provides config for the system.disk.pending_operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
            default:
              - "device"
      system.disk.weighted_io_time:
        description: "SystemDiskWeightedIoTimeMetricConfig provides config for the system.disk.weighted_io_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "device"
            default:
              - "device"
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for disk metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
