# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for postgresql metrics.
    type: object
    properties:
      postgresql.backends:
        description: "PostgresqlBackendsConfig provides config for the postgresql.backends metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.buffers.allocated:
        description: "PostgresqlBgwriterBuffersAllocatedConfig provides config for the postgresql.bgwriter.buffers.allocated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.buffers.writes:
        description: "PostgresqlBgwriterBuffersWritesConfig provides config for the postgresql.bgwriter.buffers.writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "source"
            default:
              - "source"
      postgresql.bgwriter.checkpoint.count:
        description: "PostgresqlBgwriterCheckpointCountConfig provides config for the postgresql.bgwriter.checkpoint.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
            default:
              - "type"
      postgresql.bgwriter.duration:
        description: "PostgresqlBgwriterDurationConfig provides config for the postgresql.bgwriter.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "type"
            default:
              - "type"
      postgresql.bgwriter.maxwritten:
        description: "PostgresqlBgwriterMaxwrittenConfig provides config for the postgresql.bgwriter.maxwritten metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.blks_hit:
        description: "PostgresqlBlksHitConfig provides config for the postgresql.blks_hit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.blks_read:
        description: "PostgresqlBlksReadConfig provides config for the postgresql.blks_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.blocks_read:
        description: "PostgresqlBlocksReadConfig provides config for the postgresql.blocks_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "source"
            default:
              - "source"
      postgresql.commits:
        description: "PostgresqlCommitsConfig provides config for the postgresql.commits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.connection.max:
        description: "PostgresqlConnectionMaxConfig provides config for the postgresql.connection.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.database.count:
        description: "PostgresqlDatabaseCountConfig provides config for the postgresql.database.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.database.locks:
        description: "PostgresqlDatabaseLocksConfig provides config for the postgresql.database.locks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "relation"
                - "mode"
                - "lock_type"
            default:
              - "relation"
              - "mode"
              - "lock_type"
      postgresql.db_size:
        description: "PostgresqlDbSizeConfig provides config for the postgresql.db_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.deadlocks:
        description: "PostgresqlDeadlocksConfig provides config for the postgresql.deadlocks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.function.calls:
        description: "PostgresqlFunctionCallsConfig provides config for the postgresql.function.calls metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "function"
            default:
              - "function"
      postgresql.index.scans:
        description: "PostgresqlIndexScansConfig provides config for the postgresql.index.scans metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.index.size:
        description: "PostgresqlIndexSizeConfig provides config for the postgresql.index.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.operations:
        description: "PostgresqlOperationsConfig provides config for the postgresql.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
            default:
              - "operation"
      postgresql.replication.data_delay:
        description: "PostgresqlReplicationDataDelayConfig provides config for the postgresql.replication.data_delay metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "replication_client"
            default:
              - "replication_client"
      postgresql.rollbacks:
        description: "PostgresqlRollbacksConfig provides config for the postgresql.rollbacks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rows:
        description: "PostgresqlRowsConfig provides config for the postgresql.rows metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "sum"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "state"
            default:
              - "state"
      postgresql.sequential_scans:
        description: "PostgresqlSequentialScansConfig provides config for the postgresql.sequential_scans metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.table.count:
        description: "PostgresqlTableCountConfig provides config for the postgresql.table.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.table.size:
        description: "PostgresqlTableSizeConfig provides config for the postgresql.table.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.table.vacuum.count:
        description: "PostgresqlTableVacuumCountConfig provides config for the postgresql.table.vacuum.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.temp.io:
        description: "PostgresqlTempIoConfig provides config for the postgresql.temp.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.temp_files:
        description: "PostgresqlTempFilesConfig provides config for the postgresql.temp_files metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_deleted:
        description: "PostgresqlTupDeletedConfig provides config for the postgresql.tup_deleted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_fetched:
        description: "PostgresqlTupFetchedConfig provides config for the postgresql.tup_fetched metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_inserted:
        description: "PostgresqlTupInsertedConfig provides config for the postgresql.tup_inserted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_returned:
        description: "PostgresqlTupReturnedConfig provides config for the postgresql.tup_returned metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_updated:
        description: "PostgresqlTupUpdatedConfig provides config for the postgresql.tup_updated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.wal.age:
        description: "PostgresqlWalAgeConfig provides config for the postgresql.wal.age metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.wal.delay:
        description: "PostgresqlWalDelayConfig provides config for the postgresql.wal.delay metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
                - "replication_client"
            default:
              - "operation"
              - "replication_client"
      postgresql.wal.lag:
        description: "PostgresqlWalLagConfig provides config for the postgresql.wal.lag metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "operation"
                - "replication_client"
            default:
              - "operation"
              - "replication_client"
  events_config:
    description: EventsConfig provides config for postgresql events.
    type: object
    properties:
      db.server.query_sample:
        description: EventConfig provides common config for a  db.server.query_sample event.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      db.server.top_query:
        description: EventConfig provides common config for a  db.server.top_query event.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for postgresql resource attributes.
    type: object
    properties:
      postgresql.database.name:
        description: ResourceAttributeConfig provides common config for a postgresql.database.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.index.name:
        description: ResourceAttributeConfig provides common config for a postgresql.index.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.schema.name:
        description: ResourceAttributeConfig provides common config for a postgresql.schema.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.table.name:
        description: ResourceAttributeConfig provides common config for a postgresql.table.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for postgresql metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for postgresql logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
