type: k8s_events

status:
  class: receiver
  stability:
    alpha: [logs]
  distributions: [contrib, k8s]
  codeowners:
    active: [dmitryax, TylerHelmuth]

# TODO: Update the receiver to pass the tests
tests:
  skip_lifecycle: true
