receivers:
  k8s_events/invalid_initial_lookback:
    initial_lookback: -10s

processors:
  nop:

exporters:
  nop:

service:
  pipelines:
    logs:
      receivers: [k8s_events]
      processors: [nop]
      exporters: [nop]
