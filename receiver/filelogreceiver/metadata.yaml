type: filelog

status:
  class: receiver
  stability:
    beta: [logs]
  distributions: [contrib, k8s]
  codeowners:
    active: [djaglowski, andrzej-stencel]

tests:
  config:
    include: [ testdata/simple.log ]
