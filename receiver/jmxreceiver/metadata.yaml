type: jmx

status:
  class: receiver
  stability:
    alpha: [metrics]
  distributions: [contrib, splunk, observiq, sumo]
  codeowners:
    active: [rmfitzpatrick]

tests:
  config:
    endpoint: myendpoint:12345
