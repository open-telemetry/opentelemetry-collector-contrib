type: namedpipe

status:
  class: receiver
  stability:
    alpha: [logs]
  distributions: [contrib]
  codeowners:
    active: [djaglowski]
  unsupported_platforms: [windows, darwin]

tests:
  config:
    path: /tmp/pipe
    mode: 0600
