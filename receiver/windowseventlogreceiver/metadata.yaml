type: windowseventlog

status:
  class: receiver
  stability:
    alpha: [logs]
  distributions: [splunk, observiq, sumo]
  codeowners:
    active: [djaglowski, armstrmi, pjanotti]

tests:
  config:
    channel: application
  skip_lifecycle: true
