type: k8sobjects

status:
  class: receiver
  stability:
    beta: [logs]
  distributions: [contrib, k8s]
  codeowners:
    active: [dmitryax, hvaghani221, TylerHelmuth, ChrsMark]

tests:
  config:
  skip_lifecycle: true
  goleak:
    skip: true