# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for riak metrics.
    type: object
    properties:
      riak.memory.limit:
        description: "RiakMemoryLimitConfig provides config for the riak.memory.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      riak.node.operation.count:
        description: "RiakNodeOperationCountConfig provides config for the riak.node.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      riak.node.operation.time.mean:
        description: "RiakNodeOperationTimeMeanConfig provides config for the riak.node.operation.time.mean metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      riak.node.read_repair.count:
        description: "RiakNodeReadRepairCountConfig provides config for the riak.node.read_repair.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      riak.vnode.index.operation.count:
        description: "RiakVnodeIndexOperationCountConfig provides config for the riak.vnode.index.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      riak.vnode.operation.count:
        description: "RiakVnodeOperationCountConfig provides config for the riak.vnode.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for riak resource attributes.
    type: object
    properties:
      riak.node.name:
        description: ResourceAttributeConfig provides common config for a riak.node.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for riak metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
