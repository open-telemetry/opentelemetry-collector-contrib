_format_version: "3.0"
_transform: true

services:
  - host: localhost
    name: example_service
    port: 80
    protocol: http
    routes:
      - name: example_route
        paths:
          - /mock
        strip_path: true