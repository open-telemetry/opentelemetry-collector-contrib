prometheus:
  config:
    scrape_configs:
      - job_name: 'demo'
        scrape_interval: 5s
        tls_config:
          cert_file: /nonexistentcertfile
          key_file: ./testdata/dummy-tls-key-file
