prometheus:
  config:
    scrape_configs:
      - job_name: 'demo'
        scrape_interval: 5s
        kubernetes_sd_configs:
        - role: pod
          api_server: ""
          tls_config:
            cert_file: ./testdata/dummy-tls-cert-file
