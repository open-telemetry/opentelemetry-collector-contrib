prometheus/withExtension:
  prometheus_api_server_extension:
    enabled: true
    server_config:
      endpoint: "localhost:9090"
  config:
    scrape_configs:
      - job_name: 'demo'
        scrape_interval: 5s
prometheus/withExtensionDisabled:
  prometheus_api_server_extension:
    enabled: false
  config:
    scrape_configs:
      - job_name: 'demo'
        scrape_interval: 5s
prometheus/withoutExtension:
  config:
    scrape_configs:
      - job_name: 'demo'
        scrape_interval: 5s