# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for mongodb metrics.
    type: object
    properties:
      mongodb.active.reads:
        description: "MongodbActiveReadsConfig provides config for the mongodb.active.reads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.active.writes:
        description: "MongodbActiveWritesConfig provides config for the mongodb.active.writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.cache.operations:
        description: "MongodbCacheOperationsConfig provides config for the mongodb.cache.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.count:
        description: "MongodbCollectionCountConfig provides config for the mongodb.collection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.commands.rate:
        description: "MongodbCommandsRateConfig provides config for the mongodb.commands.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.connection.count:
        description: "MongodbConnectionCountConfig provides config for the mongodb.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cursor.count:
        description: "MongodbCursorCountConfig provides config for the mongodb.cursor.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cursor.timeout.count:
        description: "MongodbCursorTimeoutCountConfig provides config for the mongodb.cursor.timeout.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.data.size:
        description: "MongodbDataSizeConfig provides config for the mongodb.data.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.database.count:
        description: "MongodbDatabaseCountConfig provides config for the mongodb.database.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.deletes.rate:
        description: "MongodbDeletesRateConfig provides config for the mongodb.deletes.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.document.operation.count:
        description: "MongodbDocumentOperationCountConfig provides config for the mongodb.document.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.extent.count:
        description: "MongodbExtentCountConfig provides config for the mongodb.extent.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.flushes.rate:
        description: "MongodbFlushesRateConfig provides config for the mongodb.flushes.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.getmores.rate:
        description: "MongodbGetmoresRateConfig provides config for the mongodb.getmores.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.global_lock.time:
        description: "MongodbGlobalLockTimeConfig provides config for the mongodb.global_lock.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.health:
        description: "MongodbHealthConfig provides config for the mongodb.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.index.access.count:
        description: "MongodbIndexAccessCountConfig provides config for the mongodb.index.access.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.index.count:
        description: "MongodbIndexCountConfig provides config for the mongodb.index.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.index.size:
        description: "MongodbIndexSizeConfig provides config for the mongodb.index.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.inserts.rate:
        description: "MongodbInsertsRateConfig provides config for the mongodb.inserts.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.acquire.count:
        description: "MongodbLockAcquireCountConfig provides config for the mongodb.lock.acquire.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.acquire.time:
        description: "MongodbLockAcquireTimeConfig provides config for the mongodb.lock.acquire.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.acquire.wait_count:
        description: "MongodbLockAcquireWaitCountConfig provides config for the mongodb.lock.acquire.wait_count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.deadlock.count:
        description: "MongodbLockDeadlockCountConfig provides config for the mongodb.lock.deadlock.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.memory.usage:
        description: "MongodbMemoryUsageConfig provides config for the mongodb.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.io.receive:
        description: "MongodbNetworkIoReceiveConfig provides config for the mongodb.network.io.receive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.io.transmit:
        description: "MongodbNetworkIoTransmitConfig provides config for the mongodb.network.io.transmit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.request.count:
        description: "MongodbNetworkRequestCountConfig provides config for the mongodb.network.request.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.object.count:
        description: "MongodbObjectCountConfig provides config for the mongodb.object.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.operation.count:
        description: "MongodbOperationCountConfig provides config for the mongodb.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.operation.latency.time:
        description: "MongodbOperationLatencyTimeConfig provides config for the mongodb.operation.latency.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.operation.repl.count:
        description: "MongodbOperationReplCountConfig provides config for the mongodb.operation.repl.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.operation.time:
        description: "MongodbOperationTimeConfig provides config for the mongodb.operation.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.page_faults:
        description: "MongodbPageFaultsConfig provides config for the mongodb.page_faults metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.queries.rate:
        description: "MongodbQueriesRateConfig provides config for the mongodb.queries.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_commands_per_sec:
        description: "MongodbReplCommandsPerSecConfig provides config for the mongodb.repl_commands_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_deletes_per_sec:
        description: "MongodbReplDeletesPerSecConfig provides config for the mongodb.repl_deletes_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_getmores_per_sec:
        description: "MongodbReplGetmoresPerSecConfig provides config for the mongodb.repl_getmores_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_inserts_per_sec:
        description: "MongodbReplInsertsPerSecConfig provides config for the mongodb.repl_inserts_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_queries_per_sec:
        description: "MongodbReplQueriesPerSecConfig provides config for the mongodb.repl_queries_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_updates_per_sec:
        description: "MongodbReplUpdatesPerSecConfig provides config for the mongodb.repl_updates_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.session.count:
        description: "MongodbSessionCountConfig provides config for the mongodb.session.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.storage.size:
        description: "MongodbStorageSizeConfig provides config for the mongodb.storage.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.updates.rate:
        description: "MongodbUpdatesRateConfig provides config for the mongodb.updates.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.uptime:
        description: "MongodbUptimeConfig provides config for the mongodb.uptime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.wtcache.bytes.read:
        description: "MongodbWtcacheBytesReadConfig provides config for the mongodb.wtcache.bytes.read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for mongodb resource attributes.
    type: object
    properties:
      server.address:
        description: ResourceAttributeConfig provides common config for a server.address resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.port:
        description: ResourceAttributeConfig provides common config for a server.port resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for mongodb metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
