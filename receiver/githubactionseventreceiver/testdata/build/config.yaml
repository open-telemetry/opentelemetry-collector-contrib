receivers:
  githubactionsevent:
    # secret: It's a Secret to Everybody
  otlp:
    protocols:
      grpc:
      http:

processors:
  # batch:

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [githubactionsevent, otlp]
      # processors: [batch]
      exporters: [debug]
