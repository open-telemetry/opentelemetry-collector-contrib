# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for icmpcheckreceiver metrics.
    type: object
    properties:
      ping.loss.ratio:
        description: "PingLossRatioMetricConfig provides config for the ping.loss.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      ping.rtt.avg:
        description: "PingRttAvgMetricConfig provides config for the ping.rtt.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      ping.rtt.max:
        description: "PingRttMaxMetricConfig provides config for the ping.rtt.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      ping.rtt.min:
        description: "PingRttMinMetricConfig provides config for the ping.rtt.min metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      ping.rtt.stddev:
        description: "PingRttStddevMetricConfig provides config for the ping.rtt.stddev metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for icmpcheckreceiver resource attributes.
    type: object
    properties:
      net.peer.ip:
        description: ResourceAttributeConfig provides common config for a net.peer.ip resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      net.peer.name:
        description: ResourceAttributeConfig provides common config for a net.peer.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for icmpcheckreceiver metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
