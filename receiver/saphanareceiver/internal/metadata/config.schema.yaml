# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for saphana metrics.
    type: object
    properties:
      saphana.alert.count:
        description: "SaphanaAlertCountConfig provides config for the saphana.alert.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.backup.latest:
        description: "SaphanaBackupLatestConfig provides config for the saphana.backup.latest metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.column.memory.used:
        description: "SaphanaColumnMemoryUsedConfig provides config for the saphana.column.memory.used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.component.memory.used:
        description: "SaphanaComponentMemoryUsedConfig provides config for the saphana.component.memory.used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.connection.count:
        description: "SaphanaConnectionCountConfig provides config for the saphana.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.cpu.used:
        description: "SaphanaCPUUsedConfig provides config for the saphana.cpu.used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.disk.size.current:
        description: "SaphanaDiskSizeCurrentConfig provides config for the saphana.disk.size.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.host.memory.current:
        description: "SaphanaHostMemoryCurrentConfig provides config for the saphana.host.memory.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.host.swap.current:
        description: "SaphanaHostSwapCurrentConfig provides config for the saphana.host.swap.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.instance.code_size:
        description: "SaphanaInstanceCodeSizeConfig provides config for the saphana.instance.code_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.instance.memory.current:
        description: "SaphanaInstanceMemoryCurrentConfig provides config for the saphana.instance.memory.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.instance.memory.shared.allocated:
        description: "SaphanaInstanceMemorySharedAllocatedConfig provides config for the saphana.instance.memory.shared.allocated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.instance.memory.used.peak:
        description: "SaphanaInstanceMemoryUsedPeakConfig provides config for the saphana.instance.memory.used.peak metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.license.expiration.time:
        description: "SaphanaLicenseExpirationTimeConfig provides config for the saphana.license.expiration.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.license.limit:
        description: "SaphanaLicenseLimitConfig provides config for the saphana.license.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.license.peak:
        description: "SaphanaLicensePeakConfig provides config for the saphana.license.peak metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.network.request.average_time:
        description: "SaphanaNetworkRequestAverageTimeConfig provides config for the saphana.network.request.average_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.network.request.count:
        description: "SaphanaNetworkRequestCountConfig provides config for the saphana.network.request.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.network.request.finished.count:
        description: "SaphanaNetworkRequestFinishedCountConfig provides config for the saphana.network.request.finished.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.replication.average_time:
        description: "SaphanaReplicationAverageTimeConfig provides config for the saphana.replication.average_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.replication.backlog.size:
        description: "SaphanaReplicationBacklogSizeConfig provides config for the saphana.replication.backlog.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.replication.backlog.time:
        description: "SaphanaReplicationBacklogTimeConfig provides config for the saphana.replication.backlog.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.row_store.memory.used:
        description: "SaphanaRowStoreMemoryUsedConfig provides config for the saphana.row_store.memory.used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.schema.memory.used.current:
        description: "SaphanaSchemaMemoryUsedCurrentConfig provides config for the saphana.schema.memory.used.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.schema.memory.used.max:
        description: "SaphanaSchemaMemoryUsedMaxConfig provides config for the saphana.schema.memory.used.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.schema.operation.count:
        description: "SaphanaSchemaOperationCountConfig provides config for the saphana.schema.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.schema.record.compressed.count:
        description: "SaphanaSchemaRecordCompressedCountConfig provides config for the saphana.schema.record.compressed.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.schema.record.count:
        description: "SaphanaSchemaRecordCountConfig provides config for the saphana.schema.record.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.code_size:
        description: "SaphanaServiceCodeSizeConfig provides config for the saphana.service.code_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.count:
        description: "SaphanaServiceCountConfig provides config for the saphana.service.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.compactors.allocated:
        description: "SaphanaServiceMemoryCompactorsAllocatedConfig provides config for the saphana.service.memory.compactors.allocated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.compactors.freeable:
        description: "SaphanaServiceMemoryCompactorsFreeableConfig provides config for the saphana.service.memory.compactors.freeable metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.effective_limit:
        description: "SaphanaServiceMemoryEffectiveLimitConfig provides config for the saphana.service.memory.effective_limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.heap.current:
        description: "SaphanaServiceMemoryHeapCurrentConfig provides config for the saphana.service.memory.heap.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.limit:
        description: "SaphanaServiceMemoryLimitConfig provides config for the saphana.service.memory.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.shared.current:
        description: "SaphanaServiceMemorySharedCurrentConfig provides config for the saphana.service.memory.shared.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.memory.used:
        description: "SaphanaServiceMemoryUsedConfig provides config for the saphana.service.memory.used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.stack_size:
        description: "SaphanaServiceStackSizeConfig provides config for the saphana.service.stack_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.service.thread.count:
        description: "SaphanaServiceThreadCountConfig provides config for the saphana.service.thread.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.transaction.blocked:
        description: "SaphanaTransactionBlockedConfig provides config for the saphana.transaction.blocked metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.transaction.count:
        description: "SaphanaTransactionCountConfig provides config for the saphana.transaction.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.uptime:
        description: "SaphanaUptimeConfig provides config for the saphana.uptime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.volume.operation.count:
        description: "SaphanaVolumeOperationCountConfig provides config for the saphana.volume.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.volume.operation.size:
        description: "SaphanaVolumeOperationSizeConfig provides config for the saphana.volume.operation.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      saphana.volume.operation.time:
        description: "SaphanaVolumeOperationTimeConfig provides config for the saphana.volume.operation.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for saphana resource attributes.
    type: object
    properties:
      db.system:
        description: ResourceAttributeConfig provides common config for a db.system resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      saphana.host:
        description: ResourceAttributeConfig provides common config for a saphana.host resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for saphana metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
