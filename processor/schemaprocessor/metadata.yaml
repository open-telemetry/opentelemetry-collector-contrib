type: schema
scope_name: otelcol/schema

status:
  class: processor
  stability:
    development: [traces, metrics, logs]
  distributions: [sumo]
  codeowners:
    active: [MovieStoreGuy]

tests:
  config:
