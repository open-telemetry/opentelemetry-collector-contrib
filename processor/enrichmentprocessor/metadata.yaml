type: enrichment

status:
  class: processor
  stability:
    alpha: [traces, metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [sandy2008]

tests:
  config:
    validate:
      error: []
