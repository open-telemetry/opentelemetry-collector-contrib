type: logdedup

status:
  class: processor
  stability:
    alpha: [logs]
  distributions: []
  warnings: []
  codeowners:
    active: [BinaryFissionGames, MikeGoldsmith, djaglowski]

tests:
  config:

telemetry:
  metrics:
    dedupe_processor_deduplicated:
      description: Number of deduplicated log records.
      unit: "{records}"
      enabled: true
      sum:
        value_type: int
        monotonic: true