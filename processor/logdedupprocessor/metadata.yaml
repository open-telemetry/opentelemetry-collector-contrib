type: logdedup

status:
  class: processor
  stability:
    alpha: [logs]
  distributions: []
  warnings: []
  codeowners:
    active: [BinaryFissionGames, MikeGoldsmith, djaglowski]

tests:
  config:

telemetry:
  metrics:
    dedupe_processor_aggregated_logs:
      description: Number of aggregated log records.
      unit: "{records}"
      enabled: true
      sum:
        value_type: int
        monotonic: true