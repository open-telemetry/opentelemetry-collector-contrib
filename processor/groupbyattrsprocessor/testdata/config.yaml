receivers:
  examplereceiver:

processors:
  groupbyattrs/custom:
    group_by_keys:
      - key1
      - key2

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [groupbyattrs/custom]
      exporters: [exampleexporter]
