type: redaction

status:
  class: processor
  stability:
    beta: [traces]
    alpha: [logs,metrics]
  distributions: [contrib, k8s]
  codeowners:
    active: [dmitryax, mx-psi, TylerHelmuth, iblancasa]
    emeritus: [leonsp-ai]

tests:
  config:
  goleak:
    ignore:
      top:
        - "sync.runtime_notifyListWait"