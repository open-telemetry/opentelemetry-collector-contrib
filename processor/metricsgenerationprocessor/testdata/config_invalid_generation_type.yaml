receivers:
  nop:

processors:
  metricsgeneration:
    rules:
      - new_metric_name: new_metric
        generation_type: invalid # invalid generation type
        operand1_metric: metric1
        operand2_metric: metric2
        operation: percent

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [metricsgeneration]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [metricsgeneration]
      exporters: [nop]
