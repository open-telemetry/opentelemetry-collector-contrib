receivers:
  nop:

processors:
  metricsgeneration:
    rules:
      # missing operand1 metric
      - name: new_metric
        type: calculate
        metric2: metric2
        operation: percent

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [metricsgeneration]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [metricsgeneration]
      exporters: [nop]
