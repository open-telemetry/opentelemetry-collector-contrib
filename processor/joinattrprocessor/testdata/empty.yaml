receivers:
  nop:

processors:
  join_attributes:

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers:
        - nop
      processors:
        - join_attributes
      exporters:
        - nop
