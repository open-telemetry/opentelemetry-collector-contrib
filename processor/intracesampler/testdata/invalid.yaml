receivers:
  nop:

processors:

  in_trace_sampler:
    scope_leaves:
      - "foo"
      - "bar"
    sampling_percentage: -15.3
    hash_seed: 22

exporters:
  nop:

service:
  pipelines:
    logs:
      receivers: [ nop ]
      processors: [ in_trace_sampler ]
      exporters: [ nop ]
