apiVersion: v1
kind: Service
metadata:
  name: {{ .Name }}-metadata
  namespace: default
spec:
  selector:
    app: {{ .Name }}-metadata
  ports:
    - name: http
      port: 8080
      targetPort: http
