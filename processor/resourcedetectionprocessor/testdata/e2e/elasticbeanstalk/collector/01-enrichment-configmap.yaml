apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Name }}-enrichment-config
  namespace: default
data:
  environment.conf: |
    {
      "deployment_id": 12345,
      "environment_name": "test-elasticbeanstalk-env",
      "version_label": "v1.0.0-test"
    }
