apiVersion: v1
kind: Service
metadata:
  name: {{ .Name }}-eks-api
  namespace: default
spec:
  selector:
    app: {{ .Name }}-eks-api
  ports:
    - name: https
      port: 8443
      targetPort: https
