# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/heroku resource attributes.
    type: object
    properties:
      cloud.provider:
        description: ResourceAttributeConfig provides common config for a cloud.provider resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      heroku.app.id:
        description: ResourceAttributeConfig provides common config for a heroku.app.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      heroku.dyno.id:
        description: ResourceAttributeConfig provides common config for a heroku.dyno.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      heroku.release.commit:
        description: ResourceAttributeConfig provides common config for a heroku.release.commit resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      heroku.release.creation_timestamp:
        description: ResourceAttributeConfig provides common config for a heroku.release.creation_timestamp resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      service.name:
        description: ResourceAttributeConfig provides common config for a service.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      service.version:
        description: ResourceAttributeConfig provides common config for a service.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
