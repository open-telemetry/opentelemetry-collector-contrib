type: resourcedetectionprocessor/k8s_api
sem_conv_version: 1.40.0

parent: resourcedetection

resource_attributes:
  k8s.cluster.uid:
    semantic_convention:
      ref: k8s.md#k8s-cluster-uid
    description: The Kubernetes cluster UID, derived from the kube-system namespace UID.
    type: string
    enabled: true
  k8s.node.name:
    semantic_convention:
      ref: k8s.md#k8s-node-name
    description: The Kubernetes node name
    type: string
    enabled: true
  k8s.node.uid:
    semantic_convention:
      ref: k8s.md#k8s-node-uid
    description: The Kubernetes node UID
    type: string
    enabled: true
