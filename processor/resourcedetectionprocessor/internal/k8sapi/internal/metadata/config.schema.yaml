# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/k8s_api resource attributes.
    type: object
    properties:
      k8s.cluster.uid:
        description: ResourceAttributeConfig provides common config for a k8s.cluster.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.node.name:
        description: ResourceAttributeConfig provides common config for a k8s.node.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.node.uid:
        description: ResourceAttributeConfig provides common config for a k8s.node.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
