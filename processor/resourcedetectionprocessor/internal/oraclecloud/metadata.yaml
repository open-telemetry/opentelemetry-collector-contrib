type: resourcedetectionprocessor/oraclecloud

parent: resourcedetection

status:
  class: processor
  stability:
    alpha: [traces, metrics, logs, profiles]
  codeowners:
    active: [dashpole]

resource_attributes:
  cloud.provider:
    description: The cloud.provider
    type: string
    enabled: true
  cloud.platform:
    description: The cloud.platform
    type: string
    enabled: true
  cloud.region:
    description: The cloud.region
    type: string
    enabled: true
  cloud.availability_zone:
    description: The cloud.availability_zone
    type: string
    enabled: true
  host.id:
    description: The host.id
    type: string
    enabled: true
  host.name:
    description: The hostname
    type: string
    enabled: true
  host.type:
    description: The instance type (shape)
    enabled: true
    type: string
  k8s.cluster.name:
    description: k8s.cluster.name
    enabled: true
    type: string
