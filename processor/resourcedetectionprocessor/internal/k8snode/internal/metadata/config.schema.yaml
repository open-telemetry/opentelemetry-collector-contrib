$defs:
  resource_attribute_config:
    description: ResourceAttributeConfig provides common config for a particular resource attribute.
    type: object
    properties:
      enabled:
        type: boolean
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/k8snode resource attributes.
    type: object
    properties:
      k8s.node.name:
        $ref: resource_attribute_config
      k8s.node.uid:
        $ref: resource_attribute_config
