# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for scalewaydetector resource attributes.
    type: object
    properties:
      cloud.account.id:
        description: ResourceAttributeConfig provides common config for a cloud.account.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.availability_zone:
        description: ResourceAttributeConfig provides common config for a cloud.availability_zone resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.platform:
        description: ResourceAttributeConfig provides common config for a cloud.platform resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.provider:
        description: ResourceAttributeConfig provides common config for a cloud.provider resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.region:
        description: ResourceAttributeConfig provides common config for a cloud.region resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.id:
        description: ResourceAttributeConfig provides common config for a host.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.image.id:
        description: ResourceAttributeConfig provides common config for a host.image.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.image.name:
        description: ResourceAttributeConfig provides common config for a host.image.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.type:
        description: ResourceAttributeConfig provides common config for a host.type resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
