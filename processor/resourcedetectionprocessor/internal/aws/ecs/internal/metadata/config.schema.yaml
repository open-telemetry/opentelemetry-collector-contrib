# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/ecs resource attributes.
    type: object
    properties:
      aws.ecs.cluster.arn:
        description: ResourceAttributeConfig provides common config for a aws.ecs.cluster.arn resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.ecs.launchtype:
        description: ResourceAttributeConfig provides common config for a aws.ecs.launchtype resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.ecs.task.arn:
        description: ResourceAttributeConfig provides common config for a aws.ecs.task.arn resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.ecs.task.family:
        description: ResourceAttributeConfig provides common config for a aws.ecs.task.family resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.ecs.task.id:
        description: ResourceAttributeConfig provides common config for a aws.ecs.task.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.ecs.task.revision:
        description: ResourceAttributeConfig provides common config for a aws.ecs.task.revision resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.log.group.arns:
        description: ResourceAttributeConfig provides common config for a aws.log.group.arns resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.log.group.names:
        description: ResourceAttributeConfig provides common config for a aws.log.group.names resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.log.stream.arns:
        description: ResourceAttributeConfig provides common config for a aws.log.stream.arns resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      aws.log.stream.names:
        description: ResourceAttributeConfig provides common config for a aws.log.stream.names resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.account.id:
        description: ResourceAttributeConfig provides common config for a cloud.account.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.availability_zone:
        description: ResourceAttributeConfig provides common config for a cloud.availability_zone resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.platform:
        description: ResourceAttributeConfig provides common config for a cloud.platform resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.provider:
        description: ResourceAttributeConfig provides common config for a cloud.provider resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.region:
        description: ResourceAttributeConfig provides common config for a cloud.region resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
