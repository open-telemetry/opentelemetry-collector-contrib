default:
all_set:
  resource_attributes:
    aws.ecs.cluster.arn:
      enabled: true
    aws.ecs.launchtype:
      enabled: true
    aws.ecs.task.arn:
      enabled: true
    aws.ecs.task.family:
      enabled: true
    aws.ecs.task.id:
      enabled: true
    aws.ecs.task.revision:
      enabled: true
    aws.log.group.arns:
      enabled: true
    aws.log.group.names:
      enabled: true
    aws.log.stream.arns:
      enabled: true
    aws.log.stream.names:
      enabled: true
    cloud.account.id:
      enabled: true
    cloud.availability_zone:
      enabled: true
    cloud.platform:
      enabled: true
    cloud.provider:
      enabled: true
    cloud.region:
      enabled: true
none_set:
  resource_attributes:
    aws.ecs.cluster.arn:
      enabled: false
    aws.ecs.launchtype:
      enabled: false
    aws.ecs.task.arn:
      enabled: false
    aws.ecs.task.family:
      enabled: false
    aws.ecs.task.id:
      enabled: false
    aws.ecs.task.revision:
      enabled: false
    aws.log.group.arns:
      enabled: false
    aws.log.group.names:
      enabled: false
    aws.log.stream.arns:
      enabled: false
    aws.log.stream.names:
      enabled: false
    cloud.account.id:
      enabled: false
    cloud.availability_zone:
      enabled: false
    cloud.platform:
      enabled: false
    cloud.provider:
      enabled: false
    cloud.region:
      enabled: false
