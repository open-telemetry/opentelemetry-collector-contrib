type: resourcedetectionprocessor/lambda
sem_conv_version: 1.40.0

parent: resourcedetection

resource_attributes:
  aws.log.group.names:
    semantic_convention:
      ref: aws.md#aws-log-group-names
    description: The aws.log.group.names
    type: slice
    enabled: true
  aws.log.stream.names:
    semantic_convention:
      ref: aws.md#aws-log-stream-names
    description: The aws.log.stream.names
    type: slice
    enabled: true
  cloud.platform:
    semantic_convention:
      ref: cloud.md#cloud-platform
    description: The cloud.platform
    type: string
    enabled: true
  cloud.provider:
    semantic_convention:
      ref: cloud.md#cloud-provider
    description: The cloud.provider
    type: string
    enabled: true
  cloud.region:
    semantic_convention:
      ref: cloud.md#cloud-region
    description: The cloud.region
    type: string
    enabled: true
  faas.instance:
    semantic_convention:
      ref: faas.md#faas-instance
    description: The faas.instance
    type: string
    enabled: true
  faas.max_memory:
    semantic_convention:
      ref: faas.md#faas-max-memory
    description: The faas.max_memory
    type: string
    enabled: true
  faas.name:
    semantic_convention:
      ref: faas.md#faas-name
    description: faas.name
    type: string
    enabled: true
  faas.version:
    semantic_convention:
      ref: faas.md#faas-version
    description: The faas.version
    type: string
    enabled: true
