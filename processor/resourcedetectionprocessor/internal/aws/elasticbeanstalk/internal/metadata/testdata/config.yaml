default:
all_set:
  resource_attributes:
    cloud.platform:
      enabled: true
    cloud.provider:
      enabled: true
    deployment.environment:
      enabled: true
    deployment.environment.name:
      enabled: true
    deployment.id:
      enabled: true
    service.instance.id:
      enabled: true
    service.version:
      enabled: true
none_set:
  resource_attributes:
    cloud.platform:
      enabled: false
    cloud.provider:
      enabled: false
    deployment.environment:
      enabled: false
    deployment.environment.name:
      enabled: false
    deployment.id:
      enabled: false
    service.instance.id:
      enabled: false
    service.version:
      enabled: false
