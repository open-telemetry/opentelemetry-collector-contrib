type: resourcedetectionprocessor/consul

parent: resourcedetection

resource_attributes:
  cloud.region:
    description: The cloud.region
    type: string
    enabled: true
  host.name:
    description: The hostname
    type: string
    enabled: true
  host.id:
    description: The host.id
    type: string
    enabled: true

entities:
  - type: host
    id_attributes: [host.id]
    descriptive_attributes: [host.name, cloud.region]
