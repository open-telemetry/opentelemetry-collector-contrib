# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/gcp resource attributes.
    type: object
    properties:
      cloud.account.id:
        description: ResourceAttributeConfig provides common config for a cloud.account.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.availability_zone:
        description: ResourceAttributeConfig provides common config for a cloud.availability_zone resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.platform:
        description: ResourceAttributeConfig provides common config for a cloud.platform resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.provider:
        description: ResourceAttributeConfig provides common config for a cloud.provider resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.region:
        description: ResourceAttributeConfig provides common config for a cloud.region resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      faas.instance:
        description: ResourceAttributeConfig provides common config for a faas.instance resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      faas.name:
        description: ResourceAttributeConfig provides common config for a faas.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      faas.version:
        description: ResourceAttributeConfig provides common config for a faas.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gcp.cloud_run.job.execution:
        description: ResourceAttributeConfig provides common config for a gcp.cloud_run.job.execution resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gcp.cloud_run.job.task_index:
        description: ResourceAttributeConfig provides common config for a gcp.cloud_run.job.task_index resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gcp.gce.instance.hostname:
        description: ResourceAttributeConfig provides common config for a gcp.gce.instance.hostname resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      gcp.gce.instance.name:
        description: ResourceAttributeConfig provides common config for a gcp.gce.instance.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      gcp.gce.instance_group_manager.name:
        description: ResourceAttributeConfig provides common config for a gcp.gce.instance_group_manager.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gcp.gce.instance_group_manager.region:
        description: ResourceAttributeConfig provides common config for a gcp.gce.instance_group_manager.region resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gcp.gce.instance_group_manager.zone:
        description: ResourceAttributeConfig provides common config for a gcp.gce.instance_group_manager.zone resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.id:
        description: ResourceAttributeConfig provides common config for a host.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.type:
        description: ResourceAttributeConfig provides common config for a host.type resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.cluster.name:
        description: ResourceAttributeConfig provides common config for a k8s.cluster.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
