# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/render resource attributes.
    type: object
    properties:
      cloud.provider:
        description: ResourceAttributeConfig provides common config for a cloud.provider resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      render.app.id:
        description: ResourceAttributeConfig provides common config for a render.app.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      render.dyno.id:
        description: ResourceAttributeConfig provides common config for a render.dyno.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      render.release.commit:
        description: ResourceAttributeConfig provides common config for a render.release.commit resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      render.release.creation_timestamp:
        description: ResourceAttributeConfig provides common config for a render.release.creation_timestamp resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      service.name:
        description: ResourceAttributeConfig provides common config for a service.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      service.version:
        description: ResourceAttributeConfig provides common config for a service.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
