# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for azuredetector resource attributes.
    type: object
    properties:
      azure.resourcegroup.name:
        description: ResourceAttributeConfig provides common config for a azure.resourcegroup.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      azure.vm.name:
        description: ResourceAttributeConfig provides common config for a azure.vm.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      azure.vm.scaleset.name:
        description: ResourceAttributeConfig provides common config for a azure.vm.scaleset.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      azure.vm.size:
        description: ResourceAttributeConfig provides common config for a azure.vm.size resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.account.id:
        description: ResourceAttributeConfig provides common config for a cloud.account.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.availability_zone:
        description: ResourceAttributeConfig provides common config for a cloud.availability_zone resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      cloud.platform:
        description: ResourceAttributeConfig provides common config for a cloud.platform resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.provider:
        description: ResourceAttributeConfig provides common config for a cloud.provider resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      cloud.region:
        description: ResourceAttributeConfig provides common config for a cloud.region resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.id:
        description: ResourceAttributeConfig provides common config for a host.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
