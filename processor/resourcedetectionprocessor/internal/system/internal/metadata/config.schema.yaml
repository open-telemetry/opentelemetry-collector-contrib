# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for resourcedetectionprocessor/system resource attributes.
    type: object
    properties:
      host.arch:
        description: ResourceAttributeConfig provides common config for a host.arch resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.cpu.cache.l2.size:
        description: ResourceAttributeConfig provides common config for a host.cpu.cache.l2.size resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.cpu.family:
        description: ResourceAttributeConfig provides common config for a host.cpu.family resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.cpu.model.id:
        description: ResourceAttributeConfig provides common config for a host.cpu.model.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.cpu.model.name:
        description: ResourceAttributeConfig provides common config for a host.cpu.model.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.cpu.stepping:
        description: ResourceAttributeConfig provides common config for a host.cpu.stepping resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.cpu.vendor.id:
        description: ResourceAttributeConfig provides common config for a host.cpu.vendor.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.id:
        description: ResourceAttributeConfig provides common config for a host.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.interface:
        description: ResourceAttributeConfig provides common config for a host.interface resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.ip:
        description: ResourceAttributeConfig provides common config for a host.ip resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.mac:
        description: ResourceAttributeConfig provides common config for a host.mac resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      os.build.id:
        description: ResourceAttributeConfig provides common config for a os.build.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      os.description:
        description: ResourceAttributeConfig provides common config for a os.description resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      os.name:
        description: ResourceAttributeConfig provides common config for a os.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      os.type:
        description: ResourceAttributeConfig provides common config for a os.type resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      os.version:
        description: ResourceAttributeConfig provides common config for a os.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
