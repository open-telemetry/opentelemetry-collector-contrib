type: resourcedetectionprocessor/system
sem_conv_version: 1.40.0

parent: resourcedetection

resource_attributes:
  host.arch:
    semantic_convention:
      ref: host.md#host-arch
    description: The host.arch
    type: string
    enabled: false
  host.cpu.cache.l2.size:
    semantic_convention:
      ref: host.md#host-cpu-cache-l2-size
    description: The host.cpu.cache.l2.size
    type: int
    enabled: false
  host.cpu.family:
    semantic_convention:
      ref: host.md#host-cpu-family
    description: The host.cpu.family
    type: string
    enabled: false
  host.cpu.model.id:
    semantic_convention:
      ref: host.md#host-cpu-model-id
    description: The host.cpu.model.id
    type: string
    enabled: false
  host.cpu.model.name:
    semantic_convention:
      ref: host.md#host-cpu-model-name
    description: The host.cpu.model.name
    type: string
    enabled: false
  host.cpu.stepping:
    semantic_convention:
      ref: host.md#host-cpu-stepping
    description: The host.cpu.stepping
    type: string
    enabled: false
  host.cpu.vendor.id:
    semantic_convention:
      ref: host.md#host-cpu-vendor-id
    description: The host.cpu.vendor.id
    type: string
    enabled: false
  host.id:
    semantic_convention:
      ref: host.md#host-id
    description: The host.id
    type: string
    enabled: false
  host.interface:
    description: Network interfaces for the host
    type: slice
    enabled: false
  host.ip:
    semantic_convention:
      ref: host.md#host-ip
    description: IP addresses for the host
    type: slice
    enabled: false
  host.mac:
    semantic_convention:
      ref: host.md#host-mac
    description: MAC addresses for the host
    type: slice
    enabled: false
  host.name:
    semantic_convention:
      ref: host.md#host-name
    description: The host.name
    type: string
    enabled: true
  os.build.id:
    semantic_convention:
      ref: os.md#os-build-id
    description: The os.build.id
    type: string
    enabled: false
  os.description:
    semantic_convention:
      ref: os.md#os-description
    description: Human readable OS version information.
    type: string
    enabled: false
  os.name:
    semantic_convention:
      ref: os.md#os-name
    description: The os.name
    type: string
    enabled: false
  os.type:
    semantic_convention:
      ref: os.md#os-type
    description: The os.type
    type: string
    enabled: true
  os.version:
    semantic_convention:
      ref: os.md#os-version
    description: The os.version
    type: string
    enabled: false
