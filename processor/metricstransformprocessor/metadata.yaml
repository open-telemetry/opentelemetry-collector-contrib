type: metricstransform
scope_name: otelcol/metricstransform

status:
  class: processor
  stability:
    beta: [metrics]
  distributions: [contrib, observiq, splunk, sumo, aws]
  codeowners:
    active: [dmitryax]

tests:
  config:
