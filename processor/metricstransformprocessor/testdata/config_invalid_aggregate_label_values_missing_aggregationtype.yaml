metrics_transform:
  transforms:
    - include: old_name
      action: update
      operations:
        - action: aggregate_label_values
          label: label1
          aggregated_values: [value1, value2]
          new_value: new_value
