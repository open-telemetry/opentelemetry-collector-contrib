receivers:
    examplereceiver:

processors:
    metricstransform:
        transforms:
            - metric_name: name1
              include: name2
              action: update

exporters:
    exampleexporter:

service:
    pipelines:
        traces:
            receivers: [examplereceiver]
            processors: [metricstransform]
            exporters: [exampleexporter]
        metrics:
            receivers: [examplereceiver]
            processors: [metricstransform]
            exporters: [exampleexporter]