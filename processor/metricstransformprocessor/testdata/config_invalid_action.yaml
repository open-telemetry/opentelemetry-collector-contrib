receivers:
    examplereceiver:

processors:
    metrics_transform:
        metric_name: old_name
        action: invalid
            

exporters:
    exampleexporter:

service:
    pipelines:
        traces:
            receivers: [examplereceiver]
            processors: [metrics_transform]
            exporters: [exampleexporter]
        metrics:
            receivers: [examplereceiver]
            processors: [metrics_transform]
            exporters: [exampleexporter]