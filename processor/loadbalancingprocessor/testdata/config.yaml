receivers:
  examplereceiver:

processors:
  loadbalancing:

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors:
      - loadbalancing
      exporters:
      - exampleexporter
