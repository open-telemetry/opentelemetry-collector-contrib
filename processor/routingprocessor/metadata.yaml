type: routing
scope_name: otelcol/routing

status:
  class: processor
  stability:
    beta: [traces, metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [jpkrohling]

tests:
  config:
