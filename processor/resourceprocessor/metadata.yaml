type: resource

status:
  class: processor
  stability:
    beta: [traces, metrics, logs]
  distributions: [core, contrib, splunk, observiq, sumo, aws, redhat, liatrio]
  codeowners:
    active: [dmitryax]

tests:
  config:
