type: resource
scope_name: otelcol/resource

status:
  class: processor
  stability:
    beta: [traces, metrics, logs]
  distributions: [core, contrib]
  codeowners:
    active: [dmitryax]

tests:
  config:
