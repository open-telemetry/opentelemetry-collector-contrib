type: resource

status:
  class: processor
  stability:
    beta: [traces, metrics, logs]
    development: [profiles]
  distributions: [core, contrib, k8s]
  codeowners:
    active: [dmitryax]

tests:
  config:
