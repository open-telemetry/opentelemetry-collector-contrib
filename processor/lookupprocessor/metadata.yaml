type: lookup

status:
  class: processor
  stability:
    development: [logs]
  distributions: []
  codeowners:
    active: [jsvd, dehaansa, VihasMakwana]

tests:
  config:
    lookups:
      - key: log.attributes["test.key"]
        attributes:
          - destination: test.result
            default: "not-found"
