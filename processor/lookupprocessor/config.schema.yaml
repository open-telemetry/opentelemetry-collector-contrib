$defs:
  source_config:
    type: object
    properties:
      type:
        description: Type is the source type identifier (e.g., "noop", "yaml", "dns").
        type: string
type: object
properties:
  source:
    $ref: source_config
