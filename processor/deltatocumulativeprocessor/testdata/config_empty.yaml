receivers:
  nop:

processors:
  deltatocumulative:

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [deltatocumulative]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [deltatocumulative]
      exporters: [nop]
