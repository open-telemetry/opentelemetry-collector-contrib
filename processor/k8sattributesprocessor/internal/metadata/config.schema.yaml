# Code generated by mdatagen. DO NOT EDIT.
$defs:
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for k8s_attributes resource attributes.
    type: object
    properties:
      container.id:
        description: ResourceAttributeConfig provides common config for a container.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.image.name:
        description: ResourceAttributeConfig provides common config for a container.image.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.image.repo_digests:
        description: ResourceAttributeConfig provides common config for a container.image.repo_digests resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      container.image.tag:
        description: ResourceAttributeConfig provides common config for a container.image.tag resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      container.image.tags:
        description: ResourceAttributeConfig provides common config for a container.image.tags resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.cluster.uid:
        description: ResourceAttributeConfig provides common config for a k8s.cluster.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.container.name:
        description: ResourceAttributeConfig provides common config for a k8s.container.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.cronjob.name:
        description: ResourceAttributeConfig provides common config for a k8s.cronjob.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.cronjob.uid:
        description: ResourceAttributeConfig provides common config for a k8s.cronjob.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.daemonset.name:
        description: ResourceAttributeConfig provides common config for a k8s.daemonset.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.daemonset.uid:
        description: ResourceAttributeConfig provides common config for a k8s.daemonset.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.deployment.name:
        description: ResourceAttributeConfig provides common config for a k8s.deployment.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.deployment.uid:
        description: ResourceAttributeConfig provides common config for a k8s.deployment.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.job.name:
        description: ResourceAttributeConfig provides common config for a k8s.job.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.job.uid:
        description: ResourceAttributeConfig provides common config for a k8s.job.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.namespace.name:
        description: ResourceAttributeConfig provides common config for a k8s.namespace.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.node.name:
        description: ResourceAttributeConfig provides common config for a k8s.node.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.node.uid:
        description: ResourceAttributeConfig provides common config for a k8s.node.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.pod.hostname:
        description: ResourceAttributeConfig provides common config for a k8s.pod.hostname resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.pod.ip:
        description: ResourceAttributeConfig provides common config for a k8s.pod.ip resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.pod.name:
        description: ResourceAttributeConfig provides common config for a k8s.pod.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.pod.start_time:
        description: ResourceAttributeConfig provides common config for a k8s.pod.start_time resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.pod.uid:
        description: ResourceAttributeConfig provides common config for a k8s.pod.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      k8s.replicaset.name:
        description: ResourceAttributeConfig provides common config for a k8s.replicaset.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.replicaset.uid:
        description: ResourceAttributeConfig provides common config for a k8s.replicaset.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.statefulset.name:
        description: ResourceAttributeConfig provides common config for a k8s.statefulset.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      k8s.statefulset.uid:
        description: ResourceAttributeConfig provides common config for a k8s.statefulset.uid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      service.name:
        description: ResourceAttributeConfig provides common config for a service.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      service.namespace:
        description: ResourceAttributeConfig provides common config for a service.namespace resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      service.version:
        description: ResourceAttributeConfig provides common config for a service.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
