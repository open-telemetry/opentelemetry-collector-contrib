receivers:
  nop:

processors:
  deltatorate:
    metrics:
      - metric1
    time_unit: invalid_unit

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [deltatorate]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [deltatorate]
      exporters: [nop]
