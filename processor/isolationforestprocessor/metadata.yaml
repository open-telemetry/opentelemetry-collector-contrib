type: isolationforest

status:
  class: processor
  stability:
    alpha: [traces, metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [your-username]

attributes:
  anomaly.isolation_score:
    description: Anomaly score between 0 and 1
    type: double
    
  anomaly.is_anomaly:
    description: Boolean classification
    type: bool
