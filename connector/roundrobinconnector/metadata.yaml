type: roundrobin

status:
  class: connector
  stability:
    beta: [traces_to_traces, metrics_to_metrics, logs_to_logs]
  distributions: [contrib, k8s]
  codeowners:
    active: [bogdandrutu]

tests:
  config:
