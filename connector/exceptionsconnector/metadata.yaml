type: exceptions

status:
  class: connector
  stability:
    alpha: [traces_to_metrics, traces_to_logs]
  distributions: [contrib, k8s]
  codeowners:
    active: [jpkrohling, marctc]

tests:
  config:
