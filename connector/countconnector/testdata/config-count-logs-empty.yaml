receivers:
  nop:

exporters:
  nop:

connectors:
  count:
    logs:

service:
  pipelines:
    logs/in:
      receivers: [nop]
      exporters: [count]
    metrics/out:
      receivers: [count]
      exporters: [nop]
