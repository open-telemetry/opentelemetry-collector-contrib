type: servicegraph
scope_name: otelcol/servicegraph

status:
  class: connector
  stability:
    alpha: [traces_to_metrics]
  distributions: [contrib]
  codeowners:
    active: [jpkrohling, mapno]

tests:
  config:
