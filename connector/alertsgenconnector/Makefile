# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Pull in the shared rules that define `fmt`, `lint`, `test`, etc.
include ../../Makefile.Common

# If you don't need any custom targets, nothing else is required here.
# The included Makefile.Common provides the `fmt` target the root build expects.
