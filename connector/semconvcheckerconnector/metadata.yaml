type: semconvchecker

status:
  class: connector
  stability:
    development: [traces_to_logs, metrics_to_logs, logs_to_logs]
  distributions: []
  codeowners:
    active: [madvikinggod, codeboten]

tests:
  config: