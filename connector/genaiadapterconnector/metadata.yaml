type: genaiadapterconnector

status:
  class: connector
  stability:
    development: [traces_to_traces, traces_to_logs]
  distributions: []
  codeowners:
    active: [liustve, jefchien, sky333999]

tests:
  config:
