type: db_storage

status:
  class: extension
  stability:
    alpha: [extension]
  distributions: [contrib]
  codeowners:
    active: [dmitryax, atoulme]

# TODO: Update the extension to make the tests pass
tests:
  skip_lifecycle: true
