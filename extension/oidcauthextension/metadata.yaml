type: oidc
scope_name: otelcol/oidcauth

status:
  class: extension
  stability:
    beta: [extension]
  distributions: [contrib, observiq, sumo]
  codeowners:
    active: [jpkrohling]

tests:
  config:
  skip_lifecycle: true
