# Example Terraform variable overrides for VPC flow fixture infrastructure.
# Copy to terraform.tfvars and adjust values as needed before applying.

project_id      = "my-gcp-project"
region          = "us-central1"
zone            = "us-central1-b"
resource_prefix = "gcp-fixture"

