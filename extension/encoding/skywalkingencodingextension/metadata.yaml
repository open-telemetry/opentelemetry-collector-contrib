display_name: Skywalking Encoding Extension
type: skywalking_encoding

description: >
  The `Skywalking` encoding extension is used to unmarshal Apache Skywalking segment traces. Marshalling is not
  supported.

status:
  disable_codecov_badge: true
  class: extension
  stability:
    alpha: [extension]
  distributions: [contrib]
  codeowners:
    active: [JaredTan95]

tests:
  config:
