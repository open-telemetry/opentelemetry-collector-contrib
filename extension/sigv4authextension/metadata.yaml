type: sigv4auth

status:
  class: extension
  stability:
    beta: [extension]
  distributions:
  - contrib
  codeowners:
    active: [Aneurysm9, erichsueh3]

tests:
  goleak:
    ignore:
      top:
        - "net/http.(*persistConn).writeLoop"
        - "internal/poll.runtime_pollWait"
  config:
