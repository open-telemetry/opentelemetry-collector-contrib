type: health_check

status:
  class: extension
  stability:
    beta: [extension]
  distributions: [core, contrib, k8s]
  codeowners:
    active: [jpkrohling]

tests:
  config:
    endpoint: localhost:0
