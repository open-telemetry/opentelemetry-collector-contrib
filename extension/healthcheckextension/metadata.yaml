type: health_check

status:
  class: extension
  stability:
    unmaintained: [extension]
  distributions: [core, contrib, k8s]
  codeowners:
    active: []
    seeking_new: true

tests:
  config:
    endpoint: localhost:0
