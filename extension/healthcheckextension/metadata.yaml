type: health_check

status:
  class: extension
  stability:
    beta: [extension]
  distributions: [core, contrib, k8s]
  codeowners:
    active: []
    seeking_new: true

tests:
  config:
    endpoint: localhost:0
