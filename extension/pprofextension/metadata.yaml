display_name: Performance Profiler Extension
type: pprof

status:
  class: extension
  stability:
    beta: [extension]
  distributions: [core, contrib, k8s]
  codeowners:
    active: [MovieStoreGuy]

tests:
  config:
