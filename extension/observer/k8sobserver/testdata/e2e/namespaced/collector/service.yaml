apiVersion: v1
kind: Service
metadata:
  name: {{ .Name }}
  namespace: default
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8888
      protocol: TCP
  selector:
    app.kubernetes.io/name: opentelemetry-collector
    app.kubernetes.io/instance: {{ .Name }}
