receivers:
  awscontainerinsight:
    collection_interval: 60s
    container_orchestrator: eks

exporters:
  debug:

service:
  pipelines:
    metrics:
      receivers: [awscontainerinsight]
      exporters: [debug]
