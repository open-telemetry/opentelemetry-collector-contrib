include-fragments = true

accept = ["200..=299", "429"]

exclude  = [
    "^http(s)?://localhost",
    "^http(s)?://example.com",
    "^https://dev.mysql.com"
]

# better to be safe and avoid failures
max-retries = 6
