# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for zookeeper metrics.
    type: object
    properties:
      zookeeper.connection.active:
        description: "ZookeeperConnectionActiveConfig provides config for the zookeeper.connection.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.data_tree.ephemeral_node.count:
        description: "ZookeeperDataTreeEphemeralNodeCountConfig provides config for the zookeeper.data_tree.ephemeral_node.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.data_tree.size:
        description: "ZookeeperDataTreeSizeConfig provides config for the zookeeper.data_tree.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.file_descriptor.limit:
        description: "ZookeeperFileDescriptorLimitConfig provides config for the zookeeper.file_descriptor.limit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.file_descriptor.open:
        description: "ZookeeperFileDescriptorOpenConfig provides config for the zookeeper.file_descriptor.open metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.follower.count:
        description: "ZookeeperFollowerCountConfig provides config for the zookeeper.follower.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.fsync.exceeded_threshold.count:
        description: "ZookeeperFsyncExceededThresholdCountConfig provides config for the zookeeper.fsync.exceeded_threshold.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.latency.avg:
        description: "ZookeeperLatencyAvgConfig provides config for the zookeeper.latency.avg metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.latency.max:
        description: "ZookeeperLatencyMaxConfig provides config for the zookeeper.latency.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.latency.min:
        description: "ZookeeperLatencyMinConfig provides config for the zookeeper.latency.min metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.packet.count:
        description: "ZookeeperPacketCountConfig provides config for the zookeeper.packet.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.request.active:
        description: "ZookeeperRequestActiveConfig provides config for the zookeeper.request.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.ruok:
        description: "ZookeeperRuokConfig provides config for the zookeeper.ruok metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.sync.pending:
        description: "ZookeeperSyncPendingConfig provides config for the zookeeper.sync.pending metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.watch.count:
        description: "ZookeeperWatchCountConfig provides config for the zookeeper.watch.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      zookeeper.znode.count:
        description: "ZookeeperZnodeCountConfig provides config for the zookeeper.znode.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for zookeeper resource attributes.
    type: object
    properties:
      server.state:
        description: ResourceAttributeConfig provides common config for a server.state resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      zk.version:
        description: ResourceAttributeConfig provides common config for a zk.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for zookeeper metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
