receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

exporters:
  nonexistent_exporter:

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [nonexistent_exporter]
