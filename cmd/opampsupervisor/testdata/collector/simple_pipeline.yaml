receivers:
  filelog:
    include: [{{.inputLogFile}}]
    start_at: "beginning"

exporters:
  file:
    path: {{.outputLogFile}}
  logging:
    verbosity: detailed

service:
  pipelines:
    logs:
      receivers: [filelog]
      exporters: [file, logging]
