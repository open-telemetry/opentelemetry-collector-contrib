// Code generated by mdatagen. DO NOT EDIT.

package main

import (
	"os"
	"path/filepath"
	"reflect"

	"github.com/open-telemetry/opentelemetry-collector-contrib/receiver/{{ .Package }}"
	"gopkg.in/yaml.v3"
)

//go:generate go run ./component_config_gen.go

func main() {

	v := reflect.ValueOf({{ .Package }}.Config{})
	fields := map[string]string{}
	for i := 0; i < v.NumField(); i++ {
		f := v.Field(i)
		fields[f.String()] = f.Type().String()
	}

	out, err := yaml.Marshal(fields)
	if err != nil {
		panic(err)
	}

	os.WriteFile(filepath.Join("..", "..", "config.yaml"), out, 0644)
}
