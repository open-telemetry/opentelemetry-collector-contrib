type: metricreceiver
metrics:
  system.cpu.time:
    description: Total CPU seconds broken down by different states.
    unit: s
    sum:
      value_type: double
      monotonic: true
      aggregation: cumulative
    attributes:
