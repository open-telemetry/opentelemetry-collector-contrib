change_type: bug_fix
component: prometheusexporter
note: "Add independent metric cleanup to prevent memory leaks when scraping stops."
issues: [41123, 43896]
subtext: "Previously, metrics were only cleaned up during a scrape. Now, a background process removes stale metrics periodically."
