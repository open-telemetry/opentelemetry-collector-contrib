change_type: enhancement

component: exporter/prometheusremotewrite

note: Add a nested `http` config block for HTTP client settings. The nested block is always available and takes precedence over flat HTTP client settings.
issues: [46209]

change_logs: [user]