change_type: enhancement

component: exporter/prometheus_remote_write

note: Add a nested `http` config block for HTTP client settings. The nested block takes precedence over flat HTTP client settings.
issues: [46209]

change_logs: [user]