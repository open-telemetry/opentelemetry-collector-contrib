# Use this changelog template to create an entry for release notes.

change_type: deprecation

component: receiver/kafka_metrics

note: Rename `kafkametrics` receiver to `kafka_metrics` with deprecated alias `kafkametrics`

issues: [45339]

subtext:

change_logs: [user]
