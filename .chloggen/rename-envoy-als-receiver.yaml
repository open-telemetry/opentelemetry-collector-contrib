# Use this changelog template to create an entry for release notes.

change_type: deprecation

component: receiver/envoy_als

note: Rename `envoyals` receiver to `envoy_als` with deprecated alias `envoyals`

issues: [45339]

subtext:

change_logs: [user]
