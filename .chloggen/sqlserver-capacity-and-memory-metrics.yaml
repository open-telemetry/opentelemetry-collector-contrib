# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. filelogreceiver)
component: receiver/sqlserver

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Add database capacity, memory, and security monitoring metrics for SQL Server

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [48457]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  Added 8 new metrics for comprehensive capacity planning, memory pressure, and security monitoring:
  - sqlserver.database.size: Total database size (data + log files)
  - sqlserver.database.data_size: Data files size only
  - sqlserver.database.transactions.active: Active transaction count per database
  - sqlserver.memory.target: Maximum memory SQL Server will use (complements existing memory.usage)
  - sqlserver.server.security.principal.count: Number of server-level security principals
  - sqlserver.server.security.role_membership.count: Number of server-level role memberships
  - sqlserver.database.security.principal.count: Number of database-level security principals
  - sqlserver.database.security.role_membership.count: Number of database-level role memberships

# If your change doesn't affect end users or the exported elements of any package,
# you should instead start your pull request title with [chore] or use the "Skip Changelog" label.
# Optional: The change log or logs in which this entry should be included.
# e.g. '[user]' or '[user, api]'
# Include 'user' if the change is relevant to end users.
# Include 'api' if there is a change to a library API.
# Default: '[user]'
change_logs: [user]
