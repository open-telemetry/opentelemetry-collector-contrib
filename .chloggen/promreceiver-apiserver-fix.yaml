change_type: breaking
component: receiver/prometheus
note: >-
  Prometheus API server config/targets stay in sync with Target Allocator changing it if both are enabled.
issues: [46086]
subtext: |-
  - API server config matches Target Allocator config logic to be optional and can be disabled by omitting the `api_server` block.
change_logs:
  - user
