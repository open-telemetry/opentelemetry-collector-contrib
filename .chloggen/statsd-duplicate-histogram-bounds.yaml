change_type: bug_fix
component: receiver/statsd
note: Reject duplicate explicit histogram bucket boundaries during configuration validation.
# Populate with the PR number once the human-authored draft PR is created.
issues: []
subtext: Previously, adjacent duplicate boundaries were accepted and could produce histogram bucket counts whose sum exceeded the total count.
change_logs: [user]
