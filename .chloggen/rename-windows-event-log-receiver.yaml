# Use this changelog template to create an entry for release notes.

change_type: deprecation

component: receiver/windows_event_log

note: Rename `windowseventlog` receiver to `windows_event_log` with deprecated alias `windowseventlog`

issues: [45339]

subtext:

change_logs: [user]
