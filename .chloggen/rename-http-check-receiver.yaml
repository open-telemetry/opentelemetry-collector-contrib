# Use this changelog template to create an entry for release notes.

change_type: deprecation

component: receiver/http_check

note: Rename `httpcheck` receiver to `http_check` with deprecated alias `httpcheck`

issues: [45339]

subtext:

change_logs: [user]
