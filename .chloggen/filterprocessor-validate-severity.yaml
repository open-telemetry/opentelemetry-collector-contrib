# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The name of the component or area affected
component: filterprocessor

# A brief description of the change
note: Fix validation of include and exclude severity configurations so they run independently of LogConditions.

# One or more related issues
issues: [46883]

# Optional: Which changelogs this entry should appear in
# 'user' indicates this affects end users
change_logs: [user]