# Use this changelog template to create an entry for release notes.

change_type: deprecation

component: receiver/udp_log

note: Rename `udplog` receiver to `udp_log` with deprecated alias `udplog`

issues: [45339]

subtext:

change_logs: [user]
