# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The name of the component, or a single word describing the area of concern
component: receiver/vcenter

# A brief description of the change
note: Skip empty vSAN SampleInfo values so timestamp parsing does not fail an entire scrape cycle.

# One or more tracking issues related to the change
issues: [47917]

# Additional optional information for the changelog entry
subtext:

# Change logs in which this entry should be included
change_logs: [user]
