# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. receiver/filelog)
component: receiver/kafka

# A brief description of the change. Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Add `partition_processing.max_in_flight` to run concurrent unmarshal-plus-Consume calls on one independent partition worker.

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [51080]

# (Optional) One or more lines of additional information to render under the primary note.
subtext: |
  When `max_in_flight` is above 1, the worker always marks after processing. `message_marking.after: false` does not mark before Consume.

# Default: '[user]'
change_logs: [user]
