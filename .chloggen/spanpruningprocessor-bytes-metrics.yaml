# Changelog entry for serialized bytes metrics in spanpruningprocessor.

change_type: enhancement

component: processor/span_pruning

note: Add opt-in enable_bytes_metrics configuration and emit bytes_received, bytes_emitted, bytes_processed_input, and bytes_processed_output telemetry counters.

issues: [49008]

change_logs: [user]
