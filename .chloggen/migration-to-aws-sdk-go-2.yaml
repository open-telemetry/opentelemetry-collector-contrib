# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern
component: awsxrayreceiver

# A brief description of the change
note: "Migrated awsxrayreceiver to AWS SDK v2 for better performance and future compatibility."

# Mandatory: One or more tracking issues related to the change
issues: [#37732]

# (Optional) Additional information
subtext: |
  - Replaced `aws-sdk-go` (v1) with `aws-sdk-go-v2`
  - Improved AWS client initialization with `config.LoadDefaultConfig`
  - Updated receiver, factory, and config files for compatibility

# The change logs in which this entry should be included
change_logs: [user]
